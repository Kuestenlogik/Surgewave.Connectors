Full-repo functional audit (2026-08-21, 15 parallel review agents over all 118 connectors).

**Summary**: 26 functional / 57 incomplete / 33 broken / 2 skeleton. 174 bug-severity findings, 27 stubs. Test quality: 18 behavioral / 55 config-only / 45 none.

The 33 broken connectors' bug-severity findings are being fixed in the follow-up commits on this issue. Recurring systemic patterns: (a) `catch (Exception) {}` per record with no logging — failures acked, offsets committed, worker retry/DLQ bypassed; (b) buffered sinks without a FlushAsync override — offsets commit while records sit in memory; (c) CDC initial snapshots that never complete for non-empty tables (MySql/Oracle/PostgreSql/SqlServer share the pattern); (d) delete/move-after-read executed before the record is durably produced.


## broken (33)

### Aws.DynamoDB  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.DynamoDB/DynamoDbSinkTask.cs` L129: Tombstone deletes can never execute: ParseRecordValue returns null for null/empty values (lines 243-244), so 'if (item == null) continue;' skips the record before the explicit tombstone DeleteRequest branch at lines 134-144 is reached — that delete path is unreachable dead code and CDC deletes are silently dropped.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.DynamoDB/DynamoDbSourceTask.cs` L140: Shards are enumerated exactly once ('if (!_shardsInitialized) { await InitializeShardsAsync(...); _shardsInitialized = true; }'); exhausted shards go to '_completedShards' and DescribeStream is never called again, so after DynamoDB's routine ~4-hour shard rollover the child shards are never discovered and the source permanently stops producing changes.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.DynamoDB/DynamoDbSinkTask.cs` L167: The UnprocessedItems loop 'while (response.UnprocessedItems.Count > 0)' retries forever with a fixed 100 ms delay — no attempt cap and no exponential backoff, so sustained throttling pins the task in a tight retry loop.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.DynamoDB/DynamoDbSinkTask.cs` L277: ConvertJsonToAttributeValue silently drops empty strings ('string.IsNullOrEmpty(str) ? null : ...'), empty arrays, and empty objects — DynamoDB has supported empty string values since 2020, so written items silently lose attributes.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.DynamoDB/DynamoDbSourceTask.cs` L172: InitializeShardsAsync reads only the first DescribeStream page (LastEvaluatedShardId pagination is not handled), so streams with more shards than one page are partially consumed.

### Aws.Kinesis  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.Kinesis/KinesisSourceTask.cs` L320: Per-record SourceOffset contains only its own shard ([$"shard:{shardId}"] = record.SequenceNumber) while all shards share one SourcePartition, and the framework's SourceOffsetStore.StoreOffsetsAsync replaces the whole offset map per partition (_cachedOffsets[key] = offset), so after a restart only the last-polled shard's sequence is restored and every other shard falls back to the default LATEST iterator, silently skipping unread records.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.Kinesis/KinesisSourceTask.cs` L181: InitializeShardsAsync skips closed shards ('if (shard.SequenceNumberRange.EndingSequenceNumber != null) continue;') even with TRIM_HORIZON/stored offsets, so records still within retention in parent shards after a reshard are never read.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Kinesis/KinesisSourceTask.cs` L134: Shard discovery runs exactly once (_shardsInitialized flag); child shards created by a reshard while the task is running are never discovered, so consumption of those shards silently stops until restart.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Kinesis/KinesisSinkTask.cs` L124: Sink is genuinely solid (PutRecords with per-entry failure retry, exponential backoff, throws after retries so offsets do not advance); only the declared BatchBytesConfig constant is never enforced anywhere.

### Aws.Neptune  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.Neptune/NeptuneSinkTask.cs` L63: PutAsync wraps every record in 'catch (Exception) { // Log and continue }' that logs nothing — any Gremlin write failure silently drops the record while the framework commits offsets, i.e. acknowledged-but-never-written data loss.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.Neptune/NeptuneSinkTask.cs` L76: Gremlin queries are built by string interpolation ('g.addV('{_vertexLabel}').property('id', '{id}'){properties}') where id, labels and property keys are never escaped (only property values get a single-quote replace), so any value containing a quote/backslash breaks the query or injects Gremlin.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.Neptune/NeptuneSourceTask.cs` L87: PollAsync swallows all exceptions with 'catch (Exception) { // Log and continue }' and returns an empty list — a permanently failing endpoint/query is indistinguishable from an idle source and no error is ever surfaced.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Neptune/NeptuneSourceTask.cs` L58: The source re-executes the same Gremlin query every poll interval and re-emits the full result set with a monotonically incrementing in-memory offset that is never restored from offset storage, producing unbounded duplicates and a meaningless offset.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Neptune/NeptuneSinkTask.cs` L29: Task reads SSL as 'ssl == "true"' with default false when the key is absent, while both connectors' ConfigDefs declare DefaultEnableSsl = true — the advertised default (SSL on, which Neptune requires) is not what the task actually does.

### Azure.Blob  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.Blob/AzureBlobSourceTask.cs` L122: Skip condition 'if (blobModified <= _lastModifiedTime && !string.IsNullOrEmpty(_lastProcessedBlob)) continue;' compares against the last enumerated blob's LastModified while listing is alphabetical, so as soon as one blob is processed, any later-listed blob with an older LastModified is silently skipped forever — data loss even in the very first run.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.Blob/AzureBlobSourceTask.cs` L140: With delete.after.read=true the blob is deleted inside PollAsync immediately after parsing, before the records are even returned to the framework, let alone durably produced — a crash between delete and produce loses the blob's data.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.Blob/AzureBlobSinkTask.cs` L193: SerializeJsonLines calls Encoding.UTF8.GetString(record.Value) and SerializeJson calls JsonDocument.Parse(r.Value) without null checks, so a tombstone (null Value) record throws and turns into a poison pill that fails every retried PutAsync for that batch.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Azure.Blob/AzureBlobSinkTask.cs` L171: The 'field' partitioner is declared and validated in AzureBlobSinkConnector (options: ["default", "time", "field"], lines 35-36/78-85) but GetPartitionKey implements it as 'PartitionerField => $"{record.Topic}/{record.Partition}"' — byte-identical to the default, no field is ever read.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Azure.Blob/AzureBlobSinkTask.cs` L178: Blob names are '{prefix}{partitionKey}/{unixMillis}.{ext}' and UploadAsync overwrites without conditions, so two flushes of the same partition within one millisecond silently overwrite the first blob.

### Beanstalkd  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Beanstalkd/BeanstalkClient.cs` L98: ReserveAsync reads the 'RESERVED <id> <bytes>' line via the buffered StreamReader but then reads the job body from the raw NetworkStream (_stream.ReadAsync) — the reader has typically already buffered the body bytes, so the raw read blocks forever or reads bytes of the next response, desyncing the protocol; job data cannot be read reliably (source path cannot work as written).
- **bug** `src/Kuestenlogik.Surgewave.Connector.Beanstalkd/BeanstalkClient.cs` L30: The StreamWriter is created without setting NewLine, so WriteLineAsync emits Environment.NewLine — on Linux that is '\n' while the beanstalkd protocol requires '\r\n' line termination, breaking every command on non-Windows hosts.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Beanstalkd/BeanstalkdSourceTask.cs` L114: PollAsync swallows every exception ('Connection issue or other error - return what we have') and there is no reconnect logic, so after any connection failure the task silently returns empty lists forever.

### Database  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Database/DatabaseConnector.cs` L152: CreateConnection relies on DbProviderFactories.GetFactory, but the project references no database provider package (csproj only references Kuestenlogik.Surgewave.Connect) and nothing anywhere (connector repo or broker repo — git grep finds no DbProviderFactories.RegisterFactory) registers a factory, and .NET's registry starts empty — so Start() always throws 'provider is not registered' for every provider and the connector cannot work as shipped.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Database/DatabaseConnector.cs` L327: BuildQuery appends 'LIMIT {n}' in all modes, which is invalid T-SQL for the default provider 'SqlClient' (SQL Server needs TOP/OFFSET-FETCH), so table-based polling would fail on the default configuration even if a factory were registered.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Database/DatabaseConnector.cs` L599: Upsert mode emits 'INSERT OR REPLACE INTO' — SQLite-only syntax, admitted in the comment 'For other databases, this would need to be database-specific' — so insert.mode=upsert fails on SqlClient, Npgsql, and MySql.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Database/DatabaseConnector.cs` L321: table.whitelist is documented as 'Comma-separated list of tables to include' but BuildQuery/GetTopic use only tables[0] — every table after the first is silently ignored.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Database/DatabaseConnector.cs` L369: DatabaseSinkConnector declares pk.mode, pk.fields, auto.create, and auto.evolve in ConfigDef, but DatabaseSinkTask never reads any of them (it only reads connection.string, db.provider, table.name.format, insert.mode, batch.size).
- **bug** `src/Kuestenlogik.Surgewave.Connector.Database/DatabaseConnector.cs` L609: Update mode blindly uses the first JSON property as the WHERE key ('Assume first column is the key'), ignoring the declared pk.fields — if the record's first field is not the primary key, the UPDATE matches and rewrites arbitrary rows.

### Discord  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Discord/DiscordSourceTask.cs` L241: PollAsync's loop 'while (_pendingRecords.TryDequeue(out var record) && records.Count < 100)' dequeues the 101st record before evaluating the count check, then exits without adding it - that message is permanently lost every time more than 100 events are pending.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Discord/DiscordSinkTask.cs` L47: Start blocks with 'Task.Run(async () => { ... await _readyTcs.Task; }).GetAwaiter().GetResult()' with no timeout or cancellation, so an invalid bot token (Ready never fires) hangs task startup forever.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Discord/DiscordSourceTask.cs` L75: Login/StartAsync run in a fire-and-forget 'Task.Run' whose exception is never observed, so a bad token or connection failure leaves the connector running but silently producing nothing.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Discord/DiscordSinkTask.cs` L65: 'if (channel == null) continue;' silently drops (and implicitly acks) every record whose channel id cannot be resolved, with no log and no error raised.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Discord/DiscordSourceConnector.cs` L29: 'discord.message.types' is declared in ConfigDef with select options (text/embed/attachment/all) but DiscordSourceTask never reads it - all message types are always emitted.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Discord/DiscordSourceTask.cs` L25: '_pendingRecords' is an unbounded ConcurrentQueue fed by gateway events with no backpressure, so memory grows without limit if polling falls behind.

### FileStream  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.FileStream/FileStreamSourceConnector.cs` L133: The source offset is taken as '_streamOffset = _stream!.Position' after StreamReader.ReadLineAsync, but StreamReader buffers ahead, so the stored position is far past the last consumed line - resuming after restart seeks beyond unread lines and silently skips them (Kafka's FileStreamSourceTask counts consumed chars for exactly this reason).
- **bug** `src/Kuestenlogik.Surgewave.Connector.FileStream/FileStreamSourceConnector.cs` L114: OpenFile is only called from Start; if the file does not exist yet, PollAsync just delays on '_reader == null' forever and never retries opening, so a file created after startup is never read.
- **bug** `src/Kuestenlogik.Surgewave.Connector.FileStream/FileStreamSinkConnector.cs` L102: 'Encoding.UTF8.GetString(record.Value)' is called without a null check, so a tombstone record (null Value) crashes PutAsync with ArgumentNullException - every other sink in the repo guards 'record.Value == null'.

### Gcp.Bigtable  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.Bigtable/BigtableSinkTask.cs` L217: FlushEntriesAsync wraps MutateRowsAsync in 'catch (Exception) { // Log and continue }' that neither logs nor rethrows, so any failed write batch is silently discarded while the worker commits the records' offsets — guaranteed silent data loss on any Bigtable error (the MutateRows per-entry response is also ignored, hiding partial failures).
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.Bigtable/BigtableSourceTask.cs` L137: BuildRowSet only continues from '_lastRowKey' in the bare else-if branch (line 165); when bigtable.rowkey.prefix or rowkey.start/end is configured, every poll re-reads the same first 'bigtable.row.limit' rows of that range forever — never progressing and emitting endless duplicates.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Gcp.Bigtable/BigtableSinkTask.cs` L203: Config declares "Write mode: set, append, increment" but the code admits 'Note: Append and Increment operations require ReadModifyWriteRow API / For simplicity, we use SetCell for all cases' — increment mode silently overwrites counters instead of incrementing them.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Bigtable/BigtableSourceTask.cs` L125: PollAsync swallows all exceptions with an empty 'catch (Exception) { // Log and continue }' (nothing is logged), so a permanently failing source spins silently returning empty batches; the per-record catch in the sink's PutAsync (BigtableSinkTask.cs:95) has the same no-op pattern.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Bigtable/BigtableSourceTask.cs` L268: SourceOffset (message_id, row_key) is written on every record but Context.OffsetStorageReader is never read in Start, so '_lastRowKey' resets on restart and the whole table is re-emitted.

### Gcp.Spanner  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.Spanner/SpannerSinkTask.cs` L158: FlushBatchAsync wraps the whole transaction (Open/Execute/Commit) in 'catch (Exception) { // Log and continue }' that neither logs nor rethrows, so any failed Spanner write batch is silently discarded while the worker commits the records' offsets — silent data loss on any write error; the per-record catch in PutAsync (line 107) is the same no-op.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Gcp.Spanner/SpannerSourceTask.cs` L113: 'var txnOptions = GetTimestampBoundOptions();' is computed and never used, and '_maxStalenessSeconds' is parsed but referenced nowhere, so the declared 'spanner.timestamp.bound' / 'spanner.max.staleness.seconds' configs have zero effect on the query.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Spanner/SpannerSinkTask.cs` L145: The comment claims 'CommitTimeout is now passed to CommitAsync' but the call is 'transaction.CommitAsync(ct)' with no timeout — '_commitTimeoutSeconds' ('spanner.commit.timeout.seconds') is parsed in Start and never applied.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Spanner/SpannerSourceTask.cs` L77: Inline credentials are written to Path.GetTempFileName() and never deleted, and GOOGLE_APPLICATION_CREDENTIALS is set process-wide (also in SpannerSinkTask.cs:60-62) — service-account JSON persists on disk and two Spanner connectors with different credentials clobber each other.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Spanner/SpannerSourceTask.cs` L135: PollAsync swallows all exceptions with an empty 'catch (Exception) { // Log and continue }' (nothing logged), and '_lastIncrementalValue' is never restored from offset storage, so restarts re-read from scratch.

### Google.Photos  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Google.Photos/GooglePhotosSourceTask.cs` L69: The OAuth path reads clientId/clientSecret into locals that are never used, then does 'credential = GoogleCredential.FromAccessToken(refreshToken)' - a refresh token is not an access token, so this auth path cannot work as written (same pattern in GooglePhotosSinkTask.cs:45-46).
- **bug** `src/Kuestenlogik.Surgewave.Connector.Google.Photos/GooglePhotosSinkTask.cs` L131: UploadBytesAsync has 'catch { // Upload failed }' returning null, and PutAsync just skips records whose upload token is null - any transient HTTP failure silently drops the record with no retry, no error raise, and the offset still advances (silent data loss).
- **bug** `src/Kuestenlogik.Surgewave.Connector.Google.Photos/GooglePhotosSourceTask.cs` L157: FetchAlbumMediaAsync and FetchAllMediaAsync execute a single Search request and never use response.NextPageToken - only the first page of media items is ever fetched, everything beyond it is never emitted.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Photos/GooglePhotosSinkTask.cs` L157: BatchCreate's response is discarded - NewMediaItemResults per-item error statuses are never inspected, so partially failed batch creates go unnoticed.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Google.Photos/GooglePhotosSourceConnector.cs` L42: google.photos.include.shared, google.photos.date.range.start and google.photos.date.range.end are declared in the ConfigDef but never read anywhere in the task - the filters have no effect.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Photos/GooglePhotosSourceTask.cs` L28: Dedup relies solely on the unbounded in-memory HashSet _processedIds and SourceOffsets are never restored via OffsetStorageReader - a restart re-emits the whole library and memory grows without bound.

### LinkedIn  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.LinkedIn/LinkedInSinkTask.cs` L85: The UGC payload uses C# property names 'comLinkedinUgcShareContent' (line 85) and 'comLinkedinUgcMemberNetworkVisibility' (line 93) with a plain CamelCase policy and no JsonPropertyName, but LinkedIn's /v2/ugcPosts API requires the dotted keys 'com.linkedin.ugc.ShareContent' / 'com.linkedin.ugc.MemberNetworkVisibility' — every post is rejected by the API.
- **bug** `src/Kuestenlogik.Surgewave.Connector.LinkedIn/LinkedInSinkTask.cs` L100: PutAsync never checks the PostAsync response status (line 98) and wraps the whole record in 'catch (Exception) { }' (lines 100-102) while FlushAsync returns CompletedTask — failed posts are silently swallowed, offsets are committed anyway, and the framework's retry/DLQ path (ConnectWorker retries on PutAsync exceptions) is bypassed: silent data loss.
- **smell** `src/Kuestenlogik.Surgewave.Connector.LinkedIn/LinkedInSourceTask.cs` L84: _verifyToken ('linkedin.webhook.verify.token', read at line 45) is never used in HandleRequestAsync — any unauthenticated POST is accepted, and the GET handler just echoes challengeCode back (line 97) instead of the HMAC-SHA256 challengeResponse LinkedIn's webhook validation requires, so registration with the real LinkedIn API cannot complete.
- **smell** `src/Kuestenlogik.Surgewave.Connector.LinkedIn/LinkedInSourceTask.cs` L35: The event channel uses BoundedChannelFullMode.DropOldest, so under load events already acknowledged to LinkedIn with HTTP 200 are silently discarded.
- **smell** `src/Kuestenlogik.Surgewave.Connector.LinkedIn/LinkedInSinkConnector.cs` L29: 'linkedin.api.version' is declared in the ConfigDef but never read by the task — the URL is hardcoded to '/v2/ugcPosts' (LinkedInSinkTask.cs:98).

### MySql  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.MySql/MySqlCdcSourceTask.cs` L131: Initial snapshot can never complete for non-empty tables: PollAsync sets `_snapshotCompleted = true` only when PerformSnapshotAsync returns 0 records, but PerformSnapshotAsync always re-runs `SELECT * FROM ...` from scratch (no cursor), so the same rows are re-emitted as duplicates on every poll forever and binlog streaming is never reached under the default snapshot.mode=initial.
- **bug** `src/Kuestenlogik.Surgewave.Connector.MySql/MySqlCdcSourceTask.cs` L157: The poll-timeout `linkedCts` created at line 148 is never passed to `_eventEnumerator.MoveNextAsync()`; with `options.Blocking = true` the poll blocks indefinitely on a quiet binlog and the `catch (OperationCanceledException) when (timeoutCts.IsCancellationRequested)` handler is unreachable.
- **stub** `src/Kuestenlogik.Surgewave.Connector.MySql/MySqlCdcSourceTask.cs` L349: `UpdatePosition` is an empty no-op with the comment 'This depends on the MySqlCdc library exposing header info', yet it is called from every binlog event branch as if it tracked position.
- **smell** `src/Kuestenlogik.Surgewave.Connector.MySql/MySqlCdcSourceTask.cs` L481: CDC row payloads use placeholder column names (`var columnName = $"col_{i}"`) because TableMapEvent column names are never resolved, so the Debezium-style before/after documents carry col_0..col_n instead of real column names and key extraction falls back to 'col_0'.
- **smell** `src/Kuestenlogik.Surgewave.Connector.MySql/MySqlCdcSourceConnector.cs` L29: ConfigDef declares `mysql.include.before.values` (IncludeBeforeValues) but the task never reads it; before-values are always included regardless of the setting.

### OneDrive  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.OneDrive/OneDriveSourceTask.cs` L207: Delta polling loses changes: items are consumed via 'deltaResponse.Value?.Take(_batchSize)' (default 10) but '_deltaLink = deltaResponse.OdataNextLink ?? deltaResponse.OdataDeltaLink' (line 226) advances past the whole page, permanently skipping every item beyond batch.size.
- **bug** `src/Kuestenlogik.Surgewave.Connector.OneDrive/OneDriveSourceTask.cs` L272: GetDriveId() falls back to the literal "me" which is then used as '_graphClient.Drives["me"]' — /drives/me is not a valid Graph endpoint (a user's default drive is /me/drive), so without an explicit drive.id every source and sink call fails (same fallback in OneDriveSinkTask.cs:281).
- **bug** `src/Kuestenlogik.Surgewave.Connector.OneDrive/OneDriveSinkTask.cs` L100: PutAsync wraps each upload in 'catch (Exception ex) { Context?.RaiseError?.Invoke(ex); }' and continues, and the Connect runner commits the consumer offset after PutAsync returns — a record whose upload failed is acknowledged and lost.
- **stub** `src/Kuestenlogik.Surgewave.Connector.OneDrive/OneDriveSinkTask.cs` L186: update.mode is fake: the create / replace / create-or-replace branches all execute the identical 'Root.ItemWithPath(itemPath).Content.PutAsync(stream)' call despite the comment 'Create new file only (fail if exists)'.
- **smell** `src/Kuestenlogik.Surgewave.Connector.OneDrive/OneDriveSourceTask.cs` L148: ListFilesAsync fetches only the first children page (Top=_batchSize, no OdataNextLink follow) and sets _initialListDone, so folders with more than batch.size files never emit the remainder and each poll re-emits the same first page.
- **smell** `src/Kuestenlogik.Surgewave.Connector.OneDrive/OneDriveSourceTask.cs` L61: Config keys 'user.id' and 'recursive' are declared in the ConfigDef and parsed into _userId/_recursive but never read anywhere afterwards — user-scoped drive access and recursive traversal are not implemented.
- **smell** `src/Kuestenlogik.Surgewave.Connector.OneDrive/OneDriveSourceTask.cs` L305: Content download failures are swallowed by a bare 'catch { // Ignore content download errors }', silently emitting metadata-only records even when include.content=true; the delta link is also held only in memory (never stored/restored via OffsetStorageReader), so every restart re-emits everything.

### Oracle  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Oracle/OracleCdcSourceTask.cs` L151: Initial snapshot never completes for non-empty tables: '_snapshotCompleted = true' is only reached when PerformSnapshotAsync returns 0 records, but PerformSnapshotAsync re-runs 'SELECT * FROM "owner"."table"' from scratch every poll with no cursor — non-empty tables loop forever re-emitting the same first batch.max.records rows and LogMiner CDC never starts.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Oracle/OracleCdcSourceTask.cs` L365: The online-mode ADD_LOGFILE PL/SQL block cannot work: 'LOGFILENAME => (SELECT member FROM v$logfile WHERE rownum = 1)' is a scalar subquery in a procedural expression (PLS-00405), and the loop 'SELECT member FROM v$logfile WHERE rownum > 1' matches zero rows by ROWNUM semantics.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Oracle/OracleCdcSourceTask.cs` L344: When QueryLogMinerContentsAsync truncates at batch.max.records (break at line 478), 'if (records.Count > 0) { _lastScn = currentScn; }' still jumps the offset to the end of the mined SCN window, silently skipping all changes beyond the cap.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Oracle/OracleCdcSourceTask.cs` L349: ORA-01291 (missing log file) is swallowed with '_lastScn = currentScn' and a '// Log gap detected, skip to current SCN' comment — a redo-log gap silently drops changes instead of failing the task.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Oracle/OracleCdcSourceTask.cs` L773: For unqualified table names the default owner is 'Environment.UserName.ToUpperInvariant()' — the connector host's OS username, which has no relation to the Oracle schema.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Oracle/OracleCdcSourceConnector.cs` L25: 'oracle.wallet.location' is declared in the ConfigDef and parsed into _walletLocation but never used — BuildConnectionString's comment admits 'Wallet authentication is configured via external TNS configuration'.

### PostgreSql  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.PostgreSql/PostgreSqlCdcSourceTask.cs` L110: Snapshot never completes for non-empty tables: 'if (snapshotRecords.Count > 0) return snapshotRecords;' precedes '_snapshotCompleted = true', and PerformSnapshotAsync re-runs 'SELECT * FROM' every poll with no cursor — the task loops re-emitting the same rows forever and replication never starts.
- **bug** `src/Kuestenlogik.Surgewave.Connector.PostgreSql/PostgreSqlCdcSourceTask.cs` L141: The poll timeout is dead code: 'timeoutCts'/'linkedCts' (lines 131-132) are never passed to '_replicationEnumerator.MoveNextAsync()' (the enumerator is bound to _replicationCts.Token from Start), so on a quiet database PollAsync blocks indefinitely and offsets/LSN feedback never advance.
- **bug** `src/Kuestenlogik.Surgewave.Connector.PostgreSql/PostgreSqlCdcSourceTask.cs` L114: The snapshot runs before StartReplicationAsync creates the replication slot (line 248), so changes committed between the snapshot read and slot creation are never captured — no exported-snapshot coordination as in Debezium.
- **stub** `src/Kuestenlogik.Surgewave.Connector.PostgreSql/PostgreSqlCdcSourceTask.cs` L373: 'include.before.values' is declared and parsed but its handling block is empty comments ('// Try to get old values if available'), so before is always null; delete records likewise ship 'Key = []' with the comment 'Key extraction would require iterating the message'.
- **smell** `src/Kuestenlogik.Surgewave.Connector.PostgreSql/PostgreSqlSinkTask.cs` L45: In the (otherwise solid) sink, 'pk.mode' is parsed and validated by the connector but never used by the task, and 'vector.index.type'/'vector.distance.metric' (lines 58-59) are read but never applied — no vector index is ever created.
- **smell** `src/Kuestenlogik.Surgewave.Connector.PostgreSql/PostgreSqlSinkTask.cs` L190: Records whose value fails JSON parsing are silently skipped ('if (values == null) continue;') and their offsets committed, with no RaiseError or DLQ signal; column set is also cached from the first record forever.

### Pulsar  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Pulsar/PulsarSourceTask.cs` L150: 'message.PublishTime.ToString("O")' — DotPulsar's IMessage.PublishTime is 'unix time in milliseconds' (a ulong per the package docs), and "O" is not a valid numeric format specifier, so every received message throws FormatException in ConvertProperties and the source fails on its first message.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Pulsar/PulsarSourceTask.cs` L106: 'DateTimeOffset.FromUnixTimeMilliseconds((long)(message.PublishTime / 1000))' divides epoch-milliseconds by 1000 and feeds the seconds value to FromUnixTimeMilliseconds, putting every record timestamp in January 1970.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Pulsar/PulsarSourceTask.cs` L113: 'await _consumer.Acknowledge(message, ...)' runs inside PollAsync before the record is ever produced to Surgewave (CommitAsync/CommitRecord are not overridden), so a produce failure or crash after the ack permanently loses the message.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Pulsar/PulsarSourceTask.cs` L57: Multi-topic support is fake: only the first entry of 'pulsar.topics' is subscribed ('// Use first topic') and 'pulsar.topics.pattern' is passed to consumerBuilder.Topic() as a literal topic name ('// Topic pattern not supported in current DotPulsar').
- **smell** `src/Kuestenlogik.Surgewave.Connector.Pulsar/PulsarSourceConnector.cs` L43: Both ConfigDefs declare auth/TLS/tuning keys (pulsar.auth.plugin.class.name, pulsar.auth.params, pulsar.tls.*, pulsar.ack.timeout.ms, pulsar.receiver.queue.size, pulsar.producer.name, pulsar.send.timeout.ms, pulsar.batching.*, pulsar.compression.type) that neither task ever reads — secured Pulsar clusters cannot be used.

### Reddit  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Reddit/RedditSourceTask.cs` L137: Source constructs `new RedditClient(appId: clientId, appSecret: clientSecret)` with no refresh/access token — Reddit.NET does not fetch tokens itself, so every API call is unauthorized; the connector meanwhile enforces username/password/user-agent as required config (RedditSourceConnector.cs:92-108) that the task never uses, and the comment 'For read-only operations, we can use just the app ID' is wrong.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Reddit/RedditSourceTask.cs` L241: PollAsync wraps each subreddit in `catch (Exception ex) { Console.Error.WriteLine(...) }`, so the permanent auth failure above (and any other error) is invisible to the framework — the source just returns empty lists forever instead of failing.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Reddit/RedditSourceTask.cs` L54: `userAgent` is read from config ('reddit.user.agent', declared Importance.High) and then never passed to RedditClient — Reddit's API rules require a distinct User-Agent.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Reddit/RedditSinkTask.cs` L85: Sink silently repurposes the 'reddit.password' key as the OAuth refresh token (`var refreshToken = config.GetValueOrDefault(RedditConnectorConfig.PasswordConfig)`), contradicting its ConfigDef description 'Reddit password for authentication'.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Reddit/RedditSourceTask.cs` L39: Deduplication lives only in in-memory `_seenPostIds`/`_seenCommentIds`; SourceOffsets are emitted but never restored (no Context.OffsetStorageReader use, `CurrentOffset` property is dead code), so a restart re-emits everything the sort listing returns.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Reddit/RedditSourceTask.cs` L272: 'reddit.time.period' is parsed into `_timePeriod` (line 86-90) but FetchPosts uses `subreddit.Posts.Top` without ever applying it, despite the ConfigDef documenting it for top/controversial sorts.

### Redis  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Redis/RedisSourceTask.cs` L190: Stream mode sets the XREADGROUP position to the last delivered entry id (`_streamOffsets[stream] = entry.Id.ToString()`, also restored as a concrete id in RestoreOffsets line 110) — but XREADGROUP with a concrete id only replays that consumer's pending-entries list, never new messages ('>' is required), so after the first non-empty batch (and after every restart) the source stops receiving new stream entries.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Redis/RedisSourceTask.cs` L284: CommitAsync calls `StreamAcknowledgeAsync(stream, _consumerGroup, lastId)` with only the last id per stream — XACK is per-id, not cumulative, so every other entry of each batch stays in the pending-entries list forever (and the swallowed RedisException catch hides any ack failure).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Redis/RedisSinkTask.cs` L126: WriteStringAsync does `Encoding.UTF8.GetString(record.Value)` without a null check, so a tombstone record (null Value) crashes the sink; JsonDocument.Parse(record.Value) in hash/stream modes has the same issue.

### Redis.List  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Redis.List/RedisListSinkTask.cs` L46: PutAsync wraps each push in `catch (Exception) { // Log and continue }` that logs nothing and rethrows nothing — any Redis failure silently drops the record while the framework goes on to commit offsets: guaranteed unnoticed data loss.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Redis.List/RedisListSourceTask.cs` L50: Source destructively pops items (`ListLeftPopAsync`/`ListRightPopAsync`) before the records are durably written to the broker, with no CommitAsync override and no LMOVE/backup-list pattern — a crash between pop and produce permanently loses those items.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Redis.List/RedisListSourceTask.cs` L9: Class doc and ConfigDef advertise blocking BLPOP/BRPOP ('Pop direction: left (BLPOP) or right (BRPOP)') but the implementation uses non-blocking LPOP/RPOP plus `Task.Delay(Math.Min(_blockingTimeoutMs, 1000))`, so 'redis.blocking.timeout.ms' only caps a sleep; the in-memory `_offset` counter also resets to 0 each restart, making emitted keys/offsets collide across runs.

### RocketChat  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.RocketChat/RocketChatSinkTask.cs` L74: Sink PutAsync has a fully empty `catch (Exception) { }` per record and never checks `response.IsSuccessStatusCode` on chat.postMessage (line 72) — failed posts (401, bad room id, server down) are silently dropped while offsets commit.
- **bug** `src/Kuestenlogik.Surgewave.Connector.RocketChat/RocketChatSourceTask.cs` L81: Source never restores stored offsets (no Context.OffsetStorageReader use despite emitting SourceOffsets) — the start position is hardcoded `since = DateTimeOffset.UtcNow.AddMinutes(-5)`, so any downtime longer than 5 minutes permanently skips the messages in between.
- **smell** `src/Kuestenlogik.Surgewave.Connector.RocketChat/RocketChatSourceTask.cs` L84: channels.history is called with fixed `count=100` and no pagination loop while `maxTime` still advances to the newest message — a burst of >100 messages in one room within a poll interval silently loses the overflow; non-success responses are also skipped with a bare `continue` (line 87-88), hiding auth failures.
- **smell** `src/Kuestenlogik.Surgewave.Connector.RocketChat/RocketChatSinkConnector.cs` L23: The sink ConfigDef never declares the 'topics' key even though the sink runner reads it (ConnectWorker.cs:1031 in the main repo) — every other sink connector in the repo declares it; the AliasField/EmojiField/AvatarField constants in RocketChatConnectorConfig.cs are dead.

### S3  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.S3/S3Connector.cs` L211: The listing cursor is `StartAfter = _lastProcessedKey` (lexicographic key order) while `_lastProcessedKey` is advanced through objects ordered by LastModified (lines 216, 241) — any new object whose key sorts before the last processed key (e.g. random/UUID names) is never listed again and is permanently skipped.
- **bug** `src/Kuestenlogik.Surgewave.Connector.S3/S3Connector.cs` L250: With delete.after.read the object is deleted (`DeleteObjectAsync`) immediately after parsing, inside PollAsync and before the records are durably produced or offsets committed — a crash after delete loses the object's data irrecoverably.
- **smell** `src/Kuestenlogik.Surgewave.Connector.S3/S3Connector.cs` L584: Both ConfigDefs offer 'parquet' and 'avro' format options (lines 49, 404) but SerializeRecords only implements jsonlines/json — choosing parquet writes plain JSON bytes into a '.parquet' object; the source likewise falls through to JSON parsing for those formats.
- **smell** `src/Kuestenlogik.Surgewave.Connector.S3/S3Connector.cs` L288: CSV parsing is a naive `Split(',')` with no quote handling, silently mis-splitting any quoted field containing a comma; `_processedKeys` (line 114) also grows without bound over the task lifetime.

### Sap.EventMesh  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Sap.EventMesh/EventMeshSinkTask.cs` L160: PublishBatchAsync wraps the POST including `response.EnsureSuccessStatusCode()` in `catch (Exception) { // Log and continue }` that logs nothing — a failed publish (auth error, 4xx/5xx, network) silently discards the whole batch while offsets commit.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sap.EventMesh/EventMeshSourceTask.cs` L105: PollAsync swallows every exception (`catch (Exception) { // Log and continue }` with no logging), so token/endpoint misconfiguration just yields empty polls forever with no visible failure.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sap.EventMesh/EventMeshSourceTask.cs` L210: CommitAsync never checks the acknowledgement response status (`await _httpClient!.SendAsync(request, ...); _pendingAcks.Clear();`) and swallows exceptions — a rejected ack still clears the pending list, leaving the broker to redeliver (duplicates) with no error surfaced.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sap.EventMesh/EventMeshSourceConnector.cs` L31: ConfigDef declares 'eventmesh.protocol' (options amqp/mqtt/rest), 'eventmesh.topic.pattern', 'eventmesh.namespace' and 'eventmesh.prefetch.count', but no task ever reads any of them — only REST queue consumption is implemented; the CloudEvents `JsonEventFormatter` field in both tasks is likewise never used.

### Sap.OData  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Sap.OData/ODataSinkTask.cs` L163: FlushBatchAsync catches all exceptions with an empty 'catch (Exception) { // Log and continue }' (and PutAsync does the same at lines 119-122), so any failed OData write is silently dropped and the framework commits offsets for records never written — data loss whenever the service errors, since ConnectWorker retries/DLQs only on a thrown PutAsync.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Sap.OData/ODataSourceTask.cs` L103: The "oauth" auth case is an empty stub ('For simplicity, assume token is pre-configured'), and the declared ConfigDef keys odata.oauth.token.url/client.id/client.secret are never read anywhere in the connector.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Sap.OData/ODataSourceTask.cs` L65: odata.use.delta is declared and parsed into _useDelta but neither _useDelta nor _deltaLink is ever used (the csproj even NoWarn's CS0169 for the unused field) — delta-link change tracking is a no-op.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sap.OData/ODataSourceTask.cs` L175: PollAsync swallows every exception with an empty 'catch (Exception) { // Log and continue }' and no logger, hiding bad credentials/URLs forever while returning [].
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sap.OData/ODataSourceTask.cs` L233: Per-record SourceOffset (incremental_value) is written but Context.OffsetStorageReader is never consulted, so a restart re-reads the entire entity set and re-emits duplicates.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sap.OData/ODataSourceTask.cs` L111: The 'sap.client' header key is read via config.GetValueOrDefault("sap.client", "100") in both tasks but is not declared in either ConfigDef.

### Sftp  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Sftp/SftpSinkTask.cs` L128: PutAsync catches SshException per record with comment 'Connection issue - will retry on next batch' but then returns normally, so the framework commits offsets for the failed (and all remaining) records of the batch — uploads lost on any connection drop, never retried.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Sftp/SftpSourceTask.cs` L180: delete.after.read/move.after.read is executed immediately after buffering the record (DeleteFile/RenameFile inside the poll loop, before PollAsync even returns), so a crash or produce failure after deletion permanently loses the file's data; SourceTask.CommitRecord/CommitAsync are not used.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Sftp/SftpSourceTask.cs` L95: Start restores _lastModifiedThreshold from OffsetStorageReader but the file listing filter (lines 162-167) only checks the in-memory _processedFiles set, size and pattern — the threshold is write-only, so after a restart every file still on the server is re-read and re-emitted as duplicates.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sftp/SftpSourceTask.cs` L196: Per-file 'catch (Exception) { // Skip files that fail to read }' silently drops unreadable files without logging or raising an error; combined with _processedFiles not being updated they are retried forever.

### Snowflake  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Snowflake/SnowflakeSinkTask.cs` L346: A stray C# directive 'using Kuestenlogik.Surgewave.Connect;' is embedded inside the MERGE SQL verbatim string (between 'USING (SELECT ...) AS source' and 'ON ...'), so every upsert/merge statement is syntactically invalid SQL and the advertised upsert/merge write modes can never work.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Snowflake/SnowflakeSinkTask.cs` L145: PutAsync begins with 'if (_connection == null || _connection.State != ConnectionState.Open || records.Count == 0) return;' — after a dropped connection every batch is silently discarded while the framework commits offsets, i.e. acknowledged data loss instead of throwing to trigger retry.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Snowflake/SnowflakeSourceTask.cs` L238: Stream mode assumes 'Stream data is automatically consumed when read' (comment at lines 239-241), but a plain 'SELECT * FROM <stream> LIMIT n' never advances a Snowflake stream offset (only DML in a transaction consumes it), so stream mode re-delivers the same change rows on every poll forever.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Snowflake/SnowflakeSinkConnector.cs` L33: ConfigDef declares 'snowflake.stage.name', 'snowflake.use.snowpipe', and 'snowflake.pipe.name' (lines 33-35) but no code outside the config class ever reads them — the advertised Snowpipe/stage bulk-loading path is unimplemented.

### SqlServer  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.SqlServer/SqlServerCdcSourceTask.cs` L129: The default 'initial' snapshot loops forever: PerformSnapshotAsync re-runs 'SELECT * FROM [schema].[table]' from scratch on every poll with no position tracking, and _snapshotCompleted is only set when a snapshot poll returns 0 records ('if (snapshotRecords.Count > 0) return snapshotRecords; _snapshotCompleted = true;'), so any non-empty table is re-emitted in full on every poll and CDC streaming is never reached.
- **bug** `src/Kuestenlogik.Surgewave.Connector.SqlServer/SqlServerCdcSourceTask.cs` L339: PollCdcChangesAsync breaks out of the table loop at _batchMaxRecords (line 334) and QueryCdcChangesAsync also truncates at the cap, yet 'if (records.Count > 0) { _lastLsn = maxLsn; }' advances the LSN to maxLsn anyway — all unread changes for remaining rows/tables in the (fromLsn, maxLsn] window are permanently skipped (data loss).

### TigerBeetle  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.TigerBeetle/TigerBeetleSinkTask.cs` L221: FlushAccountsAsync and FlushTransfersAsync (line 236) wrap the TigerBeetle writes in 'catch (Exception) { // Log and continue }' with no actual logging or rethrow, so PutAsync returns success and ConnectWorker commits the consumer offset (ConnectWorker.cs:1088-1094) — any cluster failure silently loses the whole batch.
- **bug** `src/Kuestenlogik.Surgewave.Connector.TigerBeetle/TigerBeetleSinkTask.cs` L218: The per-item error results of CreateAccountsAsync/CreateTransfersAsync are discarded ('var results = ...; // Could log errors from results'), so accounts/transfers rejected by TigerBeetle (bad ledger, duplicate id, invalid amount) are silently dropped even on a healthy cluster.
- **bug** `src/Kuestenlogik.Surgewave.Connector.TigerBeetle/TigerBeetleSourceTask.cs` L134: GetAccountTransfersAsync always queries with 'TimestampMin = 0, TimestampMax = 0' and never advances a cursor, so every balance change re-fetches and re-emits up to 8190 historical transfers for the account — unbounded duplicate records on every poll that detects a change.
- **smell** `src/Kuestenlogik.Surgewave.Connector.TigerBeetle/TigerBeetleSinkTask.cs` L92: Per-record JSON parse failures hit 'catch (Exception) { // Log and continue }' — poison records are dropped with no log and no Context.RaiseError.
- **smell** `src/Kuestenlogik.Surgewave.Connector.TigerBeetle/TigerBeetleSinkTask.cs` L28: 'tigerbeetle.max.concurrency' is declared in both sink and source ConfigDefs and parsed into a local variable (also TigerBeetleSourceTask.cs:37) that is never used — the setting has no effect.

### TimescaleDB  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.TimescaleDB/TimescaleSinkTask.cs` L184: FlushBatchAsync wraps the entire DB write (COPY/upsert) in 'catch (Exception) { // Log and continue }' with no logging or rethrow, so connection failures or constraint violations silently discard the batch while PutAsync returns success and the worker commits offsets — permanent, invisible data loss.
- **bug** `src/Kuestenlogik.Surgewave.Connector.TimescaleDB/TimescaleSourceTask.cs` L98: The incremental cursor is in-memory only: first poll sets '_lastTimestamp = DateTime.UtcNow.AddSeconds(-_lookbackSeconds)' and Context.OffsetStorageReader is never consulted even though SourceOffset["last_time"] is emitted — a restart with downtime longer than the lookback window skips rows for good, shorter downtime re-emits duplicates.
- **smell** `src/Kuestenlogik.Surgewave.Connector.TimescaleDB/TimescaleSourceTask.cs` L148: BuildQuery injects the time filter with 'sql.Replace("WHERE", $"WHERE {_timeColumn} > @lastTime AND", OrdinalIgnoreCase)', which replaces every WHERE occurrence and corrupts custom queries that contain subqueries with their own WHERE.
- **smell** `src/Kuestenlogik.Surgewave.Connector.TimescaleDB/TimescaleSinkTask.cs` L111: Per-record JSON parse errors are swallowed by an empty 'catch (Exception) { // Log and continue }' — malformed records vanish with no log or RaiseError; the source's PollAsync has the same silent catch (TimescaleSourceTask.cs:132-135).

### Twitter  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Twitter/TwitterSinkTask.cs` L85: The POST /2/tweets response is never checked ('using var response = await _httpClient.SendAsync(...)' with no IsSuccessStatusCode test) and the surrounding 'catch (Exception) { }' is completely empty, so 401/403/429 failures silently drop tweets while the records are acknowledged as delivered.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Twitter/TwitterSourceTask.cs` L113: The user-timeline mode builds 'users/{userId}/tweets?max_results=...' with no since_id or pagination_token (unlike the search branch, line 83) and adds every returned tweet unconditionally, so the same latest tweets are re-emitted as new records on every poll interval.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Twitter/TwitterSourceTask.cs` L139: All API errors are swallowed by 'catch (Exception) { // Rate limit or API error }' with no Context.RaiseError and no backoff, and non-success responses just 'return records' (line 87) — an invalid bearer token produces an eternally silent, empty source.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Twitter/TwitterSourceConnector.cs` L27: The source ConfigDef declares consumer key/secret and access token/secret (lines 27-33) but TwitterSourceTask reads only BearerToken; likewise the sink declares 'twitter.quote.tweet.field' (TwitterSinkConnector.cs:35) that TwitterSinkTask never reads.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Twitter/TwitterSinkConnector.cs` L22: The sink ConfigDef declares no 'topics' key (every other sink in the repo defines Topics as Importance.High), so validation/UI cannot surface the required consumption topics even though the raw config is passed through.

### WhatsApp  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.WhatsApp/WhatsAppSinkTask.cs` L96: The PostAsync response status is never checked and the surrounding 'catch (Exception) { }' (lines 98-100) swallows every error, so failed sends (bad token, rate limit, 4xx/5xx) are silently dropped while the worker commits offsets — the framework retry/DLQ only triggers when PutAsync throws, which it never does.
- **bug** `src/Kuestenlogik.Surgewave.Connector.WhatsApp/WhatsAppSourceTask.cs` L32: Webhook POSTs are acked with 200 (line 110) after enqueueing into an in-memory bounded channel with 'FullMode = BoundedChannelFullMode.DropOldest', so on backlog overflow or process crash already-acknowledged messages are lost and Meta will not redeliver them.
- **smell** `src/Kuestenlogik.Surgewave.Connector.WhatsApp/WhatsAppSourceTask.cs` L64: '_ = HandleRequestAsync(context)' is fire-and-forget and the handler has no catch block, so a malformed JSON payload (JsonException at line 103) faults the discarded task as an unobserved exception while still returning 200.
- **smell** `src/Kuestenlogik.Surgewave.Connector.WhatsApp/WhatsAppSourceConnector.cs` L110: 'whatsapp.access.token' is declared with Importance.High on the source connector but WhatsAppSourceTask never reads it (the webhook path needs only the verify token).

### Xmpp  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Xmpp/XmppSinkTask.cs` L79: 'if (!_isConnected) return;' after a 5-second wait silently skips the ENTIRE batch while reporting success, so the worker commits offsets and every record in the batch is lost whenever the XMPP connection is down or slow to establish.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Xmpp/XmppSinkTask.cs` L136: Per-record 'catch (Exception) { // Log and continue }' swallows SendAsync failures with no logging, committing offsets for undelivered messages and defeating the framework's retry/DLQ.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Xmpp/XmppSinkTask.cs` L30: Required config 'xmpp.domain' (Importance.High in both connectors) is read into a local but never used — the JID is built as '{username}@{host}' (line 56, comment 'Use host as domain') — and 'xmpp.port' (line 28) is parsed but never applied to the client; identical dead reads in XmppSourceTask.cs:33-35.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Xmpp/XmppSourceTask.cs` L79: '_ = ConnectAsync(config)' is fire-and-forget and ConnectAsync's 'catch (Exception) { // Log and continue }' (line 111) hides connect/auth failures without logging or Context.RaiseError, so a misconfigured source starts 'successfully' and idles forever producing nothing.

### ZeroMQ  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.ZeroMQ/ZeroMQSinkTask.cs` L287: The boolean results of '_socket.TrySendMultipartMessage(_sendTimeout, msg)' and 'TrySendFrame' (line 301) are discarded, so a timed-out or HWM-rejected send (PUSH backpressure, disconnected peer) silently drops the record while the worker commits its offset — nothing throws, so retry/DLQ never engage.
- **smell** `src/Kuestenlogik.Surgewave.Connector.ZeroMQ/ZeroMQSinkTask.cs` L304: 'catch (Exception) { // Log and continue }' per record with no actual logging hides any NetMQ send exception (e.g. REQ state-machine violations) as a silent record drop.
- **smell** `src/Kuestenlogik.Surgewave.Connector.ZeroMQ/ZeroMQSinkConnector.cs` L26: The socket-type select options advertise 'DEALER' and 'PAIR' (also ZeroMQSourceConnector.cs:27) but the tasks' switch throws ArgumentException 'Unsupported socket type' for both (ZeroMQSinkTask.cs:238, ZeroMQSourceTask.cs:49).


## skeleton (2)

### Sap.Hana  (tests: none)
- **stub** `src/Kuestenlogik.Surgewave.Connector.Sap.Hana/HanaSourceTask.cs` L68: The SAP_HANA_AVAILABLE define is commented out (line 1-2) and the csproj ships only a commented-out driver PackageReference, so Start() unconditionally throws NotSupportedException("SAP HANA driver not installed...") — the connector cannot start as shipped (same in HanaSinkTask.cs line 57).
- **bug** `src/Kuestenlogik.Surgewave.Connector.Sap.Hana/HanaSinkTask.cs` L186: FlushBatchAsync wraps the whole write in 'catch (Exception) { // Log and continue }' with no logger, so once the driver is enabled a failed batch is silently discarded while the framework commits offsets (ConnectWorker only retries/DLQs when PutAsync throws) — same swallow in PutAsync lines 139-142.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sap.Hana/HanaSourceTask.cs` L162: PollAsync swallows every exception with an empty 'catch (Exception) { // Log and continue }' — no logging exists, so connection/SQL failures leave the task looking healthy while returning [].
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sap.Hana/HanaSourceTask.cs` L271: SourceOffset carries incremental_value per record but the task never reads Context.OffsetStorageReader, so incremental reads restart from DateTime.MinValue after every task restart (full table re-emit).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sap.Hana/HanaSinkConnector.cs` L44: ConfigDef offers write-mode options ["insert", "upsert", "update"] but HanaSinkTask.BuildCommand handles "upsert"/"merge" and routes the offered "update" through the default case as a plain INSERT.

### SpaCy  (tests: none)
- **stub** `src/Kuestenlogik.Surgewave.Connector.SpaCy/SpaCySinkTask.cs` L100: The built NLP output is discarded — after BuildOutput the code says 'The output would be written to the output topic via the task context / In a full implementation, this would use a producer to write to the output topic', so the connector calls the spaCy server and then throws every result away.
- **smell** `src/Kuestenlogik.Surgewave.Connector.SpaCy/SpaCySinkConnector.cs` L23: 'spacy.output.topic' is declared Importance.High and required by Start() (lines 59-62) yet never read by the task and nothing is ever produced to it; 'spacy.batch.size' is likewise declared but unread (the task processes records one by one).
- **smell** `src/Kuestenlogik.Surgewave.Connector.SpaCy/SpaCySinkTask.cs` L103: Non-success HTTP responses are skipped ('if (!response.IsSuccessStatusCode) continue;' line 92) and 'catch (Exception) { // Log and continue }' swallows all errors without actually logging anything.


## incomplete (57)

### Akka  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Akka/AkkaClusterSourceTask.cs` L63: Cluster source cannot receive remote publishes as written: it subscribes on the LOCAL DistributedPubSub mediator ('mediator.Tell(new Subscribe(...))') of an actor system that never joins the cluster (BuildClusterClientConfig sets only 'cluster.client.initial-contacts', no seed-nodes), and the ClusterClient created at line 55 is never used; AkkaClusterSinkTask.cs:138 likewise publishes to the local mediator instead of ClusterClient.Publish — both tasks are additionally unreachable (see next finding).
- **stub** `src/Kuestenlogik.Surgewave.Connector.Akka/AkkaConnectorConfig.cs` L13: ModeConfig 'akka.mode' (comment: 'actor, cluster, streams') is declared but never read anywhere, and AkkaClusterSourceTask/AkkaClusterSinkTask/AkkaStreamsSourceTask/AkkaStreamsSinkTask are referenced by no connector (TaskClass is always AkkaSourceTask/AkkaSinkTask) — the entire cluster and streams surface is dead code with its config keys absent from both ConfigDefs.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Akka/AkkaStreamsSourceTask.cs` L59: The 'Akka Streams' source creates an ActorMaterializer that is never used to materialize any stream — it is a plain actor pushing into a ConcurrentQueue, and 'akka.stream.overflow.strategy' is ignored here (StreamSourceActor hardcodes drop-oldest at lines 226-230).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Akka/AkkaSinkTask.cs` L117: Default mode 'akka.tell.only=true' makes delivery fire-and-forget: '_targetActorSelection!.Tell(message); return;' succeeds even when the target actor does not exist (message goes to dead letters), so offsets commit with at-most-once semantics and no error signal.

### Alexa  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Alexa/AlexaSinkTask.cs` L47: When token refresh fails (EnsureAccessTokenAsync swallows every failure at lines 210-223), PutAsync does 'if (string.IsNullOrEmpty(_accessToken)) return;' — the batch is reported as successfully processed, the worker commits offsets (ConnectWorker.cs:1088-1097), and all device commands are permanently dropped with no error.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Alexa/AlexaSinkTask.cs` L74: The directive response is discarded unchecked ('using var _ = await _httpClient!.SendAsync(request, cancellationToken);') and the per-record catch at lines 76-79 says '// Log and continue' but logs nothing — 4xx/5xx and thrown failures are silently acked, bypassing the framework's retry/DLQ path.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Alexa/AlexaSourceTask.cs` L120: PollAsync wraps the entire poll in 'catch (Exception) { // Log and continue }' with no logger, so bad credentials or API errors make the source silently return empty forever with no diagnostic.

### Amqp  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Amqp/AmqpSourceTask.cs` L26: 'public override async void Start(...)' (also AmqpSinkTask.cs:22, and both Stop methods) is fire-and-forget: Start returns before the connection/channel exist, so PollAsync/PutAsync race a null '_channel', and a connection failure inside async void escapes the task framework and can crash the process instead of failing the task.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Amqp/AmqpSinkTask.cs` L152: The per-record 'catch (Exception) { // Log and continue }' (no logging) swallows publish failures — including the NullReferenceException from the Start race on '_channel!' at line 143 — so failed publishes are acked and offsets commit, silent data loss that bypasses the worker's retry/DLQ.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Amqp/AmqpSourceTask.cs` L217: CommitAsync swallows BasicAckAsync failures ('catch (Exception) { // Log and continue }') and leaves '_pendingAcks' populated; after a channel is recreated, delivery tags restart at 1 so a later cumulative 'BasicAckAsync(maxTag, true)' can ack the wrong deliveries.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Amqp/AmqpConnectorConfig.cs` L16: 'amqp.heartbeat.seconds' (RequestedHeartbeat) is read by both tasks in CreateConnectionFactory but is not declared in either connector's ConfigDef; conversely AmqpSinkTask.WaitForConfirmsAsync (AmqpSinkTask.cs:165-171) is a no-op that only resets '_pendingConfirms', making the confirm-batching machinery vestigial.

### Anthropic  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Anthropic/AnthropicSinkTask.cs` L149: PutAsync only appends to the in-memory '_buffer' until batch.size/timeout, but FlushAsync is not overridden (base is a no-op) and the worker commits consumer offsets immediately after PutAsync returns (ConnectWorker.cs:1088-1097) — up to batch.size-1 acked-but-unprocessed records are lost on crash/restart; only Stop() drains the buffer.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Anthropic/AnthropicSinkTask.cs` L344: OutputResultAsync ignores the webhook HTTP status ('await _httpClient.PostAsJsonAsync(...)' unchecked) and, with no webhook configured, writes the Claude response to Console.WriteLine — paid completion results are discarded to stdout with no produce-back-to-topic option.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Anthropic/AnthropicSinkTask.cs` L61: The 'mode' config is declared in ConfigDef and parsed into '_mode' but never read again — ProcessCompletionsAsync runs unconditionally, so the select with single option 'completions' is decorative.

### Aws.Bedrock  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.Bedrock/BedrockSinkTask.cs` L163: Same buffered-PutAsync pattern as the Anthropic sink: records accumulate in '_buffer' with no FlushAsync override while the worker commits offsets right after PutAsync (ConnectWorker.cs:1088-1097), so buffered records are acknowledged before processing and lost on crash (only Stop() flushes).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Bedrock/BedrockSinkTask.cs` L303: Embeddings parsing only handles the Titan-style root property 'embedding', yet the comment at line 281 claims '(Titan, Cohere)' support — Cohere responses use an 'embeddings' array, so those records end with 'if (embedding == null) continue;' at line 317 and are silently skipped.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Bedrock/BedrockSinkTask.cs` L421: OutputResultAsync ignores the webhook response status and defaults to Console.WriteLine when no webhook is set — model output is discarded to stdout.

### Aws.Comprehend  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.Comprehend/ComprehendSinkTask.cs` L135: Same buffered-PutAsync/no-FlushAsync-override pattern: the worker commits consumer offsets right after PutAsync returns (ConnectWorker.cs:1088-1097) while records sit unprocessed in '_buffer', so they are lost on crash/restart; only Stop() drains.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Comprehend/ComprehendSinkTask.cs` L240: The 25-document Comprehend batch limit is clamped only on the config value ('Math.Min(bs, 25)' at line 57) but ProcessBatchAsync puts every buffered record into one TextList — a single PutAsync call delivering more than 25 records (e.g. via TransactionalSinkTaskRunner batches) produces an over-limit BatchDetect* request that fails wholesale.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Comprehend/ComprehendSinkTask.cs` L333: The BatchDetect* responses' ErrorList (per-document failures) is never inspected in any of the Process*Async methods, so failed documents silently emit an empty analysis object as if they were analyzed.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Comprehend/ComprehendSinkTask.cs` L614: OutputResultAsync ignores the webhook HTTP status and falls back to Console.WriteLine — analysis results are discarded to stdout when no webhook is configured.

### Aws.Efs  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.Efs/EfsSinkTask.cs` L110: PutAsync wraps each record in 'catch (Exception) { // Log and continue }' with no logging — a failed CreateFileSystem/DeleteFileSystem/etc. is silently discarded and the record acked, bypassing the worker's retry/DLQ handling (ConnectWorker treats a returning PutAsync as success and commits offsets).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Efs/EfsSinkTask.cs` L119: ProcessOperationAsync's switch has no default case, so an unknown or misspelled 'operation' value is silently ignored rather than surfaced as an error.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Efs/EfsSourceTask.cs` L163: DescribeMountTargetsAsync and DescribeAccessPointsAsync results are taken from a single page (no Marker/NextToken loop), unlike GetFileSystemsAsync which paginates — file systems with many mount targets/access points get truncated state and can emit spurious change events.

### Aws.Sqs  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Aws.Sqs/SqsSinkTask.cs` L143: FlushBufferAsync ignores the SendMessageBatchAsync response — SQS reports per-entry failures (throttling, oversize) in response.Failed without throwing, so those messages are silently dropped while offsets are committed.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Aws.Sqs/SqsSourceTask.cs` L164: DeleteMessageBatchAsync's response.Failed is likewise unchecked in CommitAsync; failures only cause redelivery (duplicates), not loss, but are invisible.

### Azure.CosmosDb  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.CosmosDb/CosmosDbSinkTask.cs` L218: ProcessRecordAsync ends with 'catch (JsonException) { // Invalid JSON, skip this record }' — unparseable records are silently dropped with no logging or dead-lettering while offsets advance.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Azure.CosmosDb/CosmosDbSourceTask.cs` L162: When HasMoreResults goes false the iterator is rebuilt from the original _startFrom (e.g. ChangeFeedStartFrom.Beginning()) instead of the live _continuationToken, so if that branch fires with start.from=beginning the whole change feed is replayed as duplicates.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Azure.CosmosDb/CosmosDbSourceConnector.cs` L27: ConfigDef declares 'azure.cosmosdb.lease.container' and 'azure.cosmosdb.lease.prefix' but no task ever reads them — the source uses the pull model with continuation tokens, so both keys are dead config.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Azure.CosmosDb/CosmosDbSinkTask.cs` L131: Tombstone handling is inverted for delete mode: 'if (_writeMode != WriteModeDelete) { await DeleteFromKeyAsync(...) }' means in write.mode=delete a null-value tombstone is ignored instead of deleting the document.

### Azure.OpenAI  (tests: config-only)
- **stub** `src/Kuestenlogik.Surgewave.Connector.Azure.OpenAI/AzureOpenAISinkTask.cs` L602: OutputResultAsync's only destinations are an optional webhook or 'Console.WriteLine(json)' — with no webhook configured, all chat/embedding/image/transcription results are printed to stdout and discarded; there is no produce-back-to-topic path.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.OpenAI/AzureOpenAISinkTask.cs` L193: PutAsync buffers records into _buffer and only flushes on batch-size/timeout or Stop; FlushAsync is not overridden (base SinkTask.FlushAsync is a no-op), so buffered-but-unprocessed records can be sitting in memory when offsets are committed and are lost on crash.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.OpenAI/AzureOpenAISinkTask.cs` L598: The webhook result of _httpClient.PostAsJsonAsync(_webhookUrl, output, ...) is discarded — no status check, so failed webhook deliveries (4xx/5xx) are silently lost.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Azure.OpenAI/AzureOpenAISinkTask.cs` L408: SinkRecord.Value is byte[], so 'if (record.Value is byte[] bytes)' is always true for non-null values — the whisper JSON-envelope/base64 branch (lines 412-439) and ExtractInputText's ToString fallback are dead code; whisper mode always sends the raw record bytes as audio.

### Azure.ServiceBus  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.ServiceBus/ServiceBusSinkTask.cs` L119: FlushBufferAsync reuses the same ServiceBusMessageBatch after sending it on overflow ('Batch is full, send it and start a new one' — but the batch is never cleared or recreated), so all previously sent messages remain in the batch and are re-sent by every later SendMessagesAsync, duplicating messages whenever a flush exceeds one AMQP batch.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.ServiceBus/ServiceBusSourceTask.cs` L71: Stop() and Dispose() call CompletePendingMessagesAsync ('Complete any pending messages before stopping'), acknowledging PeekLock messages that were polled but not yet committed via CommitAsync — on shutdown before the records are durably written to Surgewave, those Service Bus messages are deleted and lost.

### Azure.Table  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.Table/TableStorageSinkTask.cs` L138: The tombstone-delete branch ('null/empty value = delete', lines 138-148) is unreachable dead code: ParseEntity returns null for null/empty Value and both PutAsync (line 101-103) and ProcessBatchAsync (line 133-135) 'continue' before reaching it, so tombstone records are silently dropped instead of deleting entities.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.Table/TableStorageSinkTask.cs` L176: TableTransactionFailedException derives from RequestFailedException, so a 409 in insert mode (or 404 in update mode, line 181) from SubmitTransactionAsync matches the earlier filtered catch and 'return's — the whole atomic batch (up to 100 records) is silently dropped because one entity conflicted, instead of falling through to the ProcessIndividuallyAsync fallback.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Azure.Table/TableStorageSourceTask.cs` L42: azure.table.incremental.column is declared in ConfigDef ('Column for incremental tracking') and read into _incrementalColumn, but never referenced again — BuildQueryFilter hardcodes 'Timestamp' and 'RowKey', so the configured column is ignored.

### Azure.TextAnalytics  (tests: config-only)
- **stub** `src/Kuestenlogik.Surgewave.Connector.Azure.TextAnalytics/TextAnalyticsSinkTask.cs` L535: Summarization mode is faked: comment admits 'For simplicity, we'll use key phrase extraction as a summary alternative' and the 'summary' field is just the first N key phrases joined with '. ', not the advertised extractive summarization.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.TextAnalytics/TextAnalyticsSinkTask.cs` L169: Modes 'healthcare' and 'abstractive-summarization' are offered in ConfigDef and pass connector validation but have no case in FlushBufferAsync's switch — they silently fall through to 'default: ProcessSentimentAsync', so the user gets sentiment analysis instead of the configured mode.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Azure.TextAnalytics/TextAnalyticsSinkTask.cs` L126: PutAsync only appends to _buffer and there is no FlushAsync override, so the runtime's flush/offset-commit cycle can commit offsets for records still sitting unprocessed in _buffer (only Stop() flushes) — a crash loses up to batch.size buffered records that were already acknowledged.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Azure.TextAnalytics/TextAnalyticsSinkConnector.cs` L41: pii.categories and pii.domain are declared in ConfigDef but never read anywhere — ProcessPiiAsync calls RecognizePiiEntitiesBatchAsync with only the language, so both settings are silently ignored.

### Batching  (tests: behavioral)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Batching/BatchingSinkTask.cs` L187: Completed batches are only appended to _completedBatches and delivered via GetBatches()/OnBatchReady, which nothing in the Connect runtime or this repo calls when the sink is deployed standalone — consumed records are batched into memory that grows without bound and is never delivered anywhere while the framework keeps committing offsets.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Batching/BatchingSourceTask.cs` L98: The task is fed only through the public AddMessage() method ('designed to be used in a pipeline where messages are added via the AddMessage method'), which no runtime code path invokes — deployed as a normal source connector, PollAsync never has data and produces nothing; it is an embeddable library, not a working standalone connector.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Batching/BatchingSourceTask.cs` L77: PollAsync returns an empty list immediately with no delay when the batch is not ready, so a poll loop without its own pacing busy-spins at 100% CPU.

### Cassandra  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Cassandra/CassandraSourceTask.cs` L32: The incremental cursor _lastTimestamp is tracked only in memory and Start() never reads Context.OffsetStorageReader (unlike e.g. the Azure.Table source), even though every SourceRecord writes a SourceOffset — after any task restart the timestamp filter is empty and the whole table is re-ingested from scratch.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Cassandra/CassandraSourceTask.cs` L231: In table mode without a timestamp column the query is always 'SELECT * FROM {table} LIMIT {maxRows}' with no paging state, so every poll re-emits the same first N rows forever and rows beyond cassandra.max.rows.per.poll are never delivered.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Cassandra/CassandraSinkTask.cs` L300: ParseRecordValue's 'catch { return null; }' silently drops any record whose value is not parseable JSON (CreateInsertStatementAsync then skips it) with no logging or error raise, hiding data loss for malformed records.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Cassandra/CassandraSinkTask.cs` L39: cassandra.write.mode (insert/upsert) is read into _writeMode but never used afterwards — the sink always issues a plain INSERT regardless of the configured mode.

### Elasticsearch  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Elasticsearch/ElasticsearchSourceTask.cs` L306: BuildQuery silently discards a user-supplied DSL query: 'if (_query.TrimStart().StartsWith('{')) { baseQuery = new QueryStringQuery { Query = "*" }; }' - despite ConfigDef advertising "Query string or DSL JSON to filter documents", any DSL JSON becomes match-all and exports the whole index.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Elasticsearch/ElasticsearchSourceTask.cs` L325: Incremental timestamp mode filters with 'new TermQuery { Field = new Field(_incrementalField), Value = _lastTimestampValue }' - an equality on the last seen timestamp instead of a range greater-than, so documents with newer timestamps are never returned once a cursor exists.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Elasticsearch/ElasticsearchSourceTask.cs` L228: Failed searches are swallowed silently ('if (!searchResponse.IsValidResponse) return records;' here and at lines 241/280) with no log or RaiseError, so a misconfigured index/auth yields an empty stream forever with no diagnostic.

### Facebook  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Facebook/FacebookSinkTask.cs` L88: PutAsync wraps each Graph API post in an empty 'catch (Exception) { }' and never checks the HTTP response status (line 86), so failed posts (bad token, rate limit, API error) are silently dropped while the records are acked.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Facebook/FacebookSinkTask.cs` L81: post.type 'photo'/'video' posts to '{pageId}/photos' / '{pageId}/videos' with form data containing only access_token/message/link and no media url or source, which the Graph API rejects - these modes cannot work as written and the failure is swallowed.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Facebook/FacebookSourceTask.cs` L108: The webhook handler returns 200 to Facebook right after enqueueing into an in-memory bounded channel with 'FullMode = BoundedChannelFullMode.DropOldest' (line 33), so acknowledged events are silently discarded under backlog or lost on crash and Facebook will never redeliver them.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Facebook/FacebookSourceConnector.cs` L25: Source ConfigDef declares 'facebook.access.token' (Importance.High), 'facebook.include.comments' and 'facebook.include.reactions', but FacebookSourceTask never reads any of them - it is webhook-only and never calls the Graph API.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Facebook/FacebookConnectorConfig.cs` L15: 'facebook.app.secret' is defined but never used - the webhook accepts POSTs without X-Hub-Signature-256 validation, so anyone who can reach the port can inject fake events.

### FacebookMessenger  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.FacebookMessenger/MessengerSinkTask.cs` L82: PutAsync swallows every send failure in an empty 'catch (Exception) { }' and never inspects the response of PostAsync (line 77), so undelivered messages are silently lost while the records are acked.
- **bug** `src/Kuestenlogik.Surgewave.Connector.FacebookMessenger/MessengerSourceTask.cs` L106: Webhook POSTs are acknowledged with 200 after enqueueing into an in-memory channel configured 'FullMode = BoundedChannelFullMode.DropOldest' (line 32), so acknowledged messages can be silently dropped under backlog or lost on crash with no redelivery.
- **smell** `src/Kuestenlogik.Surgewave.Connector.FacebookMessenger/MessengerSinkConnector.cs` L33: 'messenger.message.type' is declared and read into _messageType (MessengerSinkTask.cs:38) but never applied - the payload is always 'message = new { text }' regardless of the configured type.
- **smell** `src/Kuestenlogik.Surgewave.Connector.FacebookMessenger/MessengerSourceConnector.cs` L25: Source ConfigDef declares 'messenger.page.access.token' (Importance.High) and 'messenger.app.secret', but MessengerSourceTask reads neither - no Graph API call and no webhook signature validation exist.
- **smell** `src/Kuestenlogik.Surgewave.Connector.FacebookMessenger/MessengerSinkTask.cs` L78: The page access token is passed in the URL query string ('me/messages?access_token={_accessToken}') instead of a header or body, exposing the credential to any URL-logging proxy or diagnostics.

### Gcp.BigQuery  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.BigQuery/BigQuerySinkTask.cs` L152: Retry classifier lower-cases the message ('var message = ex.Message.ToLowerInvariant()') then tests 'message.Contains("backendError")' with a capital E, so BigQuery backendError responses can never be classified as retriable.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.BigQuery/BigQuerySourceTask.cs` L174: Query-mode SQL rewriting is broken: 'query.Replace(" WHERE ", ...)' replaces EVERY occurrence (injecting the timestamp predicate into subqueries too), and line 190 appends ' LIMIT {n}' unconditionally, producing invalid SQL when the user query already contains LIMIT — and the resulting failure is only written to Console.Error each poll.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.BigQuery/BigQuerySourceTask.cs` L34: Incremental position '_lastTimestamp' is in-memory only — SourceOffset is emitted on every record but Context.OffsetStorageReader is never consulted in Start, so every task restart re-reads and re-emits all rows matching the query.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.BigQuery/BigQuerySinkTask.cs` L174: 'catch (JsonException) { // Invalid JSON, skip this record }' silently drops non-JSON records with no log or DLQ, and streaming inserts use SkipInvalidRows=true while ignoring the InsertRowsAsync result, so per-row insert problems are invisible.

### Gcp.Firestore  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.Firestore/FirestoreSinkTask.cs` L327: ConvertJsonObject replaces ANY JSON object containing numeric 'lat' and 'lng' keys with a new dictionary holding only '["_geopoint"] = new GeoPoint(lat, lng)', silently discarding every other field of that object — and even the intended geo case is written nested under an artificial "_geopoint" key instead of as the field value.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Firestore/FirestoreSinkTask.cs` L187: 'catch (JsonException) { // Invalid JSON, skip this record }' silently drops unparseable records with no logging or DLQ while their offsets are committed.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Firestore/FirestoreSourceTask.cs` L239: '_lastDocumentId' is restored from offset storage and updated every poll but never used in any query cursor (no StartAfter), so in poll mode without 'firestore.timestamp.field' the same first N documents are re-emitted on every poll interval.

### Gcp.Language  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.Language/LanguageSinkTask.cs` L215: ModeSyntax calls '_client.AnalyzeSentimentAsync(document, ...)' instead of AnalyzeSyntaxAsync (copy-paste), so the advertised 'syntax' mode returns sentence sentiment, never syntax tokens.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.Language/LanguageSinkTask.cs` L120: PutAsync buffers records ('_buffer.AddRange(records)') and only processes when batch size/timeout hit, but the task never overrides FlushAsync — the Connect worker calls FlushAsync then commits the offset after every record (ConnectWorker.cs:1095-1097), so up to batch.size-1 consumed records sit unanalyzed in memory with their offsets already committed and are lost on restart.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Language/LanguageSinkTask.cs` L379: Webhook delivery ignores the HTTP response ('await _httpClient.PostAsJsonAsync(...)' with no status check), so 4xx/5xx results are silently dropped.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Language/LanguageSinkConnector.cs` L20: ConfigDef declares 'gcp.project.id' but neither the connector nor LanguageSinkTask ever reads it (LanguageServiceClientBuilder is built without it).

### Gcp.PubSub  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.PubSub/PubSubSourceTask.cs` L132: With auto-ack enabled — and DefaultAutoAck = true (PubSubConnectorConfig.cs:35) — each message is AcknowledgeAsync'd inside PollAsync before the record is ever produced to Surgewave, giving at-most-once delivery by default: a crash between ack and produce permanently loses the message.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.PubSub/PubSubSourceTask.cs` L93: Stop() and Dispose call AcknowledgePendingMessages(), acking every pending ackId even though those messages have not been confirmed committed to Surgewave (CommitAsync was never called for them) — a mid-cycle shutdown acknowledges undelivered messages.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.PubSub/PubSubSourceConnector.cs` L33: 'gcp.pubsub.ack.deadline.seconds' is declared in the source ConfigDef and 'gcp.pubsub.batch.delay.ms' in the sink ConfigDef (PubSubSinkConnector.cs:35), but neither key is ever read by the tasks (grep over the tasks finds no usage).

### Gcp.VertexAI  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.VertexAI/VertexAISinkConnector.cs` L36: The 'mode' ConfigDef offers EditorHint.Select options ["chat", "embeddings"] but validation only accepts "completions" or "embeddings" (Start throws 'Invalid mode: chat'), so picking the first dropdown value in the UI always fails.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.VertexAI/VertexAISinkTask.cs` L186: PutAsync buffers records ('_buffer.AddRange(records)') without overriding FlushAsync, and the Connect worker commits the offset right after FlushAsync for every record (ConnectWorker.cs:1095-1097), so up to batch.size-1 consumed records sit unprocessed in memory with committed offsets and are lost on restart.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.VertexAI/VertexAISinkTask.cs` L473: Webhook delivery ignores the HTTP response ('await _httpClient.PostAsJsonAsync(...)' with no status check), so failed deliveries are silently dropped.

### Git  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Git/GitSinkTask.cs` L106: If the repository path does not exist, Start() leaves _repository null (it only opens 'if (Directory.Exists(_repositoryPath))') and PutAsync does 'if (_repository == null) return Task.CompletedTask;' - records are acknowledged as delivered while nothing is ever written, silent data loss on misconfiguration instead of failing the task.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Git/GitSinkConnector.cs` L25: OutputMode ConfigDef uses 'EditorHint.Select, options: ["file", "branch"]' but the implemented values are 'write'/'append' (OutputModeWrite/OutputModeAppend) - the UI dropdown offers only invalid values.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Git/GitSourceConnector.cs` L44: git.ssh.key.path and git.ssh.key.passphrase are declared in both source and sink ConfigDefs but never read by either task - SSH authentication is unimplemented (sink only supports UsernamePasswordCredentials).
- **stub** `src/Kuestenlogik.Surgewave.Connector.Git/GitSourceConnector.cs` L38: Source declares git.remote ('Remote name for fetch operations'), git.username and git.password, but GitSourceTask never performs any fetch and never reads these keys - it only reads commits already present in the local repository.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Git/GitSourceTask.cs` L179: PollAsync swallows RepositoryNotFoundException and LibGit2SharpException with only 'await Task.Delay(5000); return [];' - no logging or Context error raise, so a corrupt/unreadable repo yields a silently empty source forever.

### Google.Drive  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Google.Drive/GoogleDriveSourceTask.cs` L163: ListFilesAsync requests 'nextPageToken' in Fields but never reads result.NextPageToken or sets request.PageToken - with PageSize = batch.size (default 10) only the first page is ever emitted, so folders with more files than one page silently miss data (also affects the initial listing in watch mode).
- **stub** `src/Kuestenlogik.Surgewave.Connector.Google.Drive/GoogleDriveSourceTask.cs` L258: 'recursive' config is declared and parsed but BuildQuery adds the identical "'{_folderId}' in parents" clause in both branches, with the comment 'Note: For deep recursion, would need to traverse folder structure' - recursion is unimplemented.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Drive/GoogleDriveSourceTask.cs` L43: SourceOffset dictionaries are attached to every record but Start() never consults Context.OffsetStorageReader and _startPageToken is not persisted - after a restart the task re-lists and re-emits everything (duplicates).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Drive/GoogleDriveSourceTask.cs` L309: Content download uses 'catch { // Ignore content download errors }' - a record configured to include content is silently emitted without it on any download failure.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Drive/GoogleDriveSinkTask.cs` L130: Content decoding tries 'Convert.FromBase64String(contentStr)' first and only falls back to UTF-8 on exception - plain-text content that happens to be valid base64 (e.g. 'abcd') is decoded into different bytes than the original, silent corruption.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Drive/GoogleDriveSinkTask.cs` L140: ProcessRecordAsync silently returns (record acked) on invalid JSON ('catch (JsonException) { // Invalid JSON, skip record }') or missing filename/content fields - no error raise, no dead-letter, records vanish.

### Google.Home  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Google.Home/GoogleHomeSinkTask.cs` L92: PutAsync wraps each record in 'catch (Exception) { // Log and continue }' with no actual logging and no Context error raise - a failed ReportStateAndNotification call is swallowed and the record acknowledged, silent data loss on any API error.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Home/GoogleHomeSourceTask.cs` L133: PollAsync and GetDeviceListAsync swallow all exceptions ('catch (Exception) { // Log and continue }') with nothing logged - wrong credentials or a dead HomeGraph endpoint make the source silently produce nothing forever.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Home/GoogleHomeSinkTask.cs` L12: Doc claims the task 'controls Google Home smart home devices', but the implementation only calls Devices.ReportStateAndNotification (line 90), which reports state into Google's Home Graph cache - the HomeGraph API cannot actuate devices, so the sink cannot do what it advertises.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Home/GoogleHomeSourceTask.cs` L166: GetDeviceTypeAsync issues a full Devices.Sync request per device per poll, in addition to the Sync in GetDeviceListAsync - N+1 API round-trips every poll cycle.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Google.Home/GoogleHomeSourceTask.cs` L82: When the poll interval has not elapsed, PollAsync returns [] immediately instead of delaying (other connectors Task.Delay) - the worker's poll loop busy-spins between intervals.

### GraphQL  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.GraphQL/GraphQLSourceTask.cs` L231: RunSubscriptionAsync's reconnect loop only catches OperationCanceledException and WebSocketException; the InvalidOperationException thrown at line 165 ('Connection not acknowledged') or any JsonException escapes the loop, faulting the background task which is never observed - PollSubscriptionAsync keeps returning [] forever, the source dies silently.
- **smell** `src/Kuestenlogik.Surgewave.Connector.GraphQL/GraphQLSourceTask.cs` L339: Cursor tracking is off-by-one: CreateRecord(element) is called before _lastTimestamp/_lastId are updated from that element, so each record's SourceOffset carries the previous record's cursor - after restart the last committed record is re-fetched (duplicate).
- **smell** `src/Kuestenlogik.Surgewave.Connector.GraphQL/GraphQLSourceTask.cs` L325: GraphQL response errors are dropped with comment '// Log errors but don't fail' but nothing is logged and no error is raised (same for subscription 'error' messages at line 219) - a permanently failing query looks like an empty source.
- **smell** `src/Kuestenlogik.Surgewave.Connector.GraphQL/GraphQLSinkTask.cs` L126: Stop() flushes via 'FlushBufferAsync(CancellationToken.None).GetAwaiter().GetResult()' - sync-over-async blocking on shutdown.

### Grok  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Grok/GrokSinkTask.cs` L145: The task buffers records in _buffer but does not override FlushAsync (the SinkTask base implementation is a no-op), so when the worker flushes and commits offsets, records still sitting in the buffer are counted as delivered - they are lost on restart/rebalance.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Grok/GrokSinkTask.cs` L315: OutputResultAsync posts results via '_httpClient.PostAsJsonAsync(_webhookUrl, output, ...)' without checking the response status or retrying - a failing webhook silently loses every processed result.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Grok/GrokSinkTask.cs` L319: Without a webhook URL the sink's only output is 'Console.WriteLine(json)' - completion results go to stdout, not to any durable destination (documented in the ConfigDef, but effectively a demo path).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Grok/GrokSinkTask.cs` L137: Stop() drains the buffer via 'FlushBufferAsync(CancellationToken.None).GetAwaiter().GetResult()' - sync-over-async blocking on shutdown.

### Http  (tests: behavioral)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Http/HttpSourceTask.cs` L357: PollSseAsync only leaves SSE mode when the reader task 'IsFaulted || IsCanceled'; a graceful server close (ReadLineAsync returns null -> break -> task RanToCompletion, channel completed) keeps _sseMode true with an exhausted channel, so the source returns [] forever and never reconnects - despite Last-Event-ID reconnection support existing for the fault path.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Http/HttpSinkTask.cs` L129: No null-value guard: a tombstone record hits 'Encoding.UTF8.GetString(record.Value)' in SendBatchAsync (and 'new ByteArrayContent(body)' with null in SendWithRetryAsync), throwing ArgumentNullException and failing the whole batch.

### HttpServer  (tests: behavioral)
- **bug** `src/Kuestenlogik.Surgewave.Connector.HttpServer/HttpServerSourceTask.cs` L184: HandleRequest builds SourcePartition via direct indexers `_config[HttpServerConnectorConfig.Host]` / `[Port]`, but the Connect worker does not merge ConfigDef defaults into task configs (verified in ConnectWorker.cs ~line 321) and Start() itself falls back to defaults via TryGetValue — so with host/port left to their declared defaults every incoming request throws KeyNotFoundException and no record is ever produced.
- **smell** `src/Kuestenlogik.Surgewave.Connector.HttpServer/HttpServerSourceTask.cs` L197: The HTTP caller receives 202 Accepted (`Results.Accepted(... status = "accepted")`) while the record only sits in the in-memory unbounded `_pendingRecords` ConcurrentQueue; a crash before the next PollAsync loses already-acknowledged requests.

### Hue  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Hue/HueSinkTask.cs` L94: PutAsync wraps each Hue command in `catch (Exception) { // Log and continue }` with a completely empty body — failed light/group commands are silently dropped and their offsets committed, and despite the comment nothing is ever logged (Context.RaiseError is not invoked either).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Hue/HueSourceTask.cs` L118: PollAsync swallows all bridge errors with an empty `catch (Exception) { // Log and continue }` — connectivity/auth failures against the bridge are completely invisible.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Hue/HueSourceConnector.cs` L36: `hue.include.scenes` is declared in the ConfigDef ("Include scene information") but HueSourceTask never reads it and never emits scenes — the option has no effect.

### HuggingFace  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.HuggingFace/HuggingFaceSinkTask.cs` L174: PutAsync buffers records in `_buffer` until batch size/timeout but the task never overrides FlushAsync (the SinkTask base is a no-op) — ConnectWorker calls FlushAsync then immediately CommitAsync, so offsets are committed for buffered records that were never sent to the API and are lost on crash/rebalance.
- **smell** `src/Kuestenlogik.Surgewave.Connector.HuggingFace/HuggingFaceSinkTask.cs` L494: OutputResultAsync posts results with `_webhookClient.PostAsJsonAsync(...)` and never checks the response status — a webhook returning 4xx/5xx silently loses the inference result.
- **smell** `src/Kuestenlogik.Surgewave.Connector.HuggingFace/HuggingFaceSinkConnector.cs` L64: `source.language` and `target.language` are declared in the ConfigDef for translation mode but never read anywhere in the task — they have no effect on requests.

### ICal  (tests: config-only)
- **stub** `src/Kuestenlogik.Surgewave.Connector.ICal/ICalSinkTask.cs` L108: The DEFAULT output mode 'record' is unimplemented: PutAsync only handles file mode with the comment "Record mode would emit the ICS as the transformed record value / This is handled in FlushAsync", but FlushAsync (line 120) also only handles OutputModeFile — with the default config every consumed record is silently discarded.
- **smell** `src/Kuestenlogik.Surgewave.Connector.ICal/ICalSourceTask.cs` L244: `last_event_uid` is written into SourceOffset but Start() only restores `last_poll` (lines 94-99) and the `_emittedEventUids` dedup set is memory-only — every restart re-emits all events in the time window as duplicates.
- **smell** `src/Kuestenlogik.Surgewave.Connector.ICal/ICalSourceTask.cs` L193: Calendar parsing errors are swallowed by a bare `catch (Exception) { // Calendar parsing error }` returning [] with no logging — a permanently malformed feed fails silently forever.
- **smell** `src/Kuestenlogik.Surgewave.Connector.ICal/ICalSinkTask.cs` L262: FlushCalendarAsync writes with File.WriteAllTextAsync and then resets the calendar (line 265) — when `ical.output.path` contains no ${timestamp} placeholder, each rotation overwrites the file and all previously flushed events are lost despite the 'max.events.per.file rotation' claim.
- **smell** `src/Kuestenlogik.Surgewave.Connector.ICal/ICalSinkConnector.cs` L41: `ical.flush.interval.ms` is declared in the ConfigDef ("Flush interval in milliseconds for file mode") but the sink task never reads it — flushing happens only on max-events or the framework's FlushAsync.

### Imap  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Imap/ImapSourceTask.cs` L262: PostProcessMessagesAsync (delete+expunge / move, lines 496-499) runs inside PollAsync immediately after fetch, BEFORE the returned records are durably produced, and CommitAsync (line 271) is a no-op — a crash between expunge and produce permanently loses the emails with imap.delete.after.read/move.after.read enabled.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Imap/ImapSourceConnector.cs` L47: `imap.poll.interval.ms`, `imap.use.idle`, `imap.idle.timeout.minutes`, `imap.folders` and `imap.recursive` are all declared in the ConfigDef but have no effect: the task never throttles PollAsync, `_useIdle` is parsed (task lines 107-109) but never referenced, IMAP IDLE is never used, and only the single `imap.folder` is monitored.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Imap/ImapSourceTask.cs` L271: UID progress is memory-only: SourceOffset is populated (lines 461-465) and a CurrentOffset property exists, but Start() never restores from Context.OffsetStorageReader and CommitAsync says "Offset is tracked in memory" — every restart re-emits messages (with start.from=earliest, the whole mailbox).

### InProc  (tests: behavioral)
- **bug** `src/Kuestenlogik.Surgewave.Connector.InProc/InProcSourceTask.cs` L186: PollSharedMemory starts at `var position = 0` on every call, never persists a read cursor and never marks data consumed — the same shared-memory messages are re-emitted as duplicates on every poll, indefinitely.
- **bug** `src/Kuestenlogik.Surgewave.Connector.InProc/InProcSinkTask.cs` L146: PutSharedMemory wraps `_sharedMemoryPosition` back to 0 when full and overwrites unread data with no head/tail coordination with the reader (and stale length-prefixed bytes beyond the new end marker remain readable) — messages are silently lost/corrupted in shared-memory mode.
- **smell** `src/Kuestenlogik.Surgewave.Connector.InProc/InProcSinkTask.cs` L119: `_pipeConnected` is set true after the first WaitForConnectionAsync and never reset on client disconnect — after a pipe client drops, every PutAsync throws on the broken pipe and the sink never re-accepts a connection until task restart.

### InfluxDB  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.InfluxDB/InfluxDBSourceTask.cs` L156: Incremental polling queries `range(start: {_lastTimestamp:yyyy-MM-ddTHH:mm:ss.fffZ})` — Flux range start is inclusive and the ns-precision timestamp is truncated to milliseconds, so the newest row(s) are re-fetched and re-emitted as duplicates on every single poll cycle.
- **bug** `src/Kuestenlogik.Surgewave.Connector.InfluxDB/InfluxDBSinkTask.cs` L240: ParseTimestamp interprets a numeric timestamp field as Windows FILETIME via `DateTime.FromFileTimeUtc(ticks)` (also line 251 for JSON numbers) — a Unix epoch-millis value like 1755772800000 becomes a date in January 1601, corrupting the timestamps of all points whose `timestamp.field` is numeric.
- **smell** `src/Kuestenlogik.Surgewave.Connector.InfluxDB/InfluxDBSourceTask.cs` L271: `_lastTimestamp` is memory-only — SourceOffset is written (line 195) but never restored in Start() and CommitAsync is a no-op ("Offsets tracked internally"), so a restart re-reads the entire configured time range again.

### Instagram  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Instagram/InstagramSinkTask.cs` L98: PutAsync has a completely empty `catch (Exception) { }`, skips failed container creation with `if (!containerResponse.IsSuccessStatusCode) continue;` (line 79) and never checks `publishResponse` at all (lines 93-96) — failed Instagram posts are silently dropped while their offsets are committed.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Instagram/InstagramSinkTask.cs` L38: `instagram.media.type` (ConfigDef options image/video/carousel, InstagramSinkConnector.cs:33) is parsed into `_mediaType` but never used — only the image flow exists, and the `instagram.video.url.field` config constant is never wired anywhere.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Instagram/InstagramSourceTask.cs` L105: Webhook POSTs are ingested without X-Hub-Signature-256 validation (the `instagram.app.secret` constant exists but is never declared in a ConfigDef nor used) — any unauthenticated POST to the endpoint becomes a 'genuine' Instagram event record.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Instagram/InstagramSourceConnector.cs` L25: The source ConfigDef declares `instagram.access.token` (Importance.High) and `instagram.api.version`, but the webhook-only source task never uses either — declared config with no effect.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Instagram/InstagramSourceTask.cs` L35: The event channel is bounded(1000) with `FullMode = BoundedChannelFullMode.DropOldest` — under backpressure, events already acknowledged to Meta with HTTP 200 are silently discarded.

### Logic  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Logic/AggregateConnector.cs` L174: FlushAsync snapshots and clears _states BEFORE producing (lines 174-178) and the timer callback swallows every error ('catch { // Ignore flush errors }', lines 139-142), so if ProduceAsync fails the window's aggregates are permanently lost without any trace.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Logic/ScheduleTriggerConnector.cs` L68: TaskConfigs hardcodes '[ScheduleTriggerConfig.Timezone] = "UTC"' and Start never reads the timezone config, so the declared 'schedule.timezone' setting (e.g. 'Europe/Berlin') never reaches the task and cron triggers always fire in UTC.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Logic/MapConnector.cs` L66: TaskConfigs hardcodes '[MapConfig.KeyExpression] = ""' (Start never reads the key), so the declared 'map.key.expression' config is dead — key transformation can never execute even though MapTask implements it.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Logic/SplitConnector.cs` L64: TaskConfigs hardcodes '[SplitConfig.IncludeIndex] = "false"' and Start never reads it, so the declared 'split.include.index' config can never activate the implemented _index header feature.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Logic/FilterConnector.cs` L38: 'config.GetValueOrDefault(FilterConfig.Condition, "") ?? throw new ArgumentException(...)' can never throw because the non-null default makes GetValueOrDefault never return null — the same dead 'required' validation pattern repeats in IfConnector.cs:41, MapConnector.cs:41, SwitchConnector.cs:41, AggregateConnector.cs:52 and SplitConnector.cs:41, so empty required configs pass validation.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Logic/SwitchConnector.cs` L178: 'current?.GetValue<string>()' throws for non-string JSON values (numbers, booleans), which the surrounding catch converts to null — switching on a numeric field like '{"type": 1}' always falls through to the default topic instead of matching case '1'.

### Matter  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Matter/MatterSinkTask.cs` L75: PutAsync wraps each record in 'catch (Exception) { // Log and continue }' with no actual logging, and none of the PostAsync calls (lines 97, 114, 130, ...) check the response status — failed device commands are silently dropped, the record is committed as delivered, and the framework's retry/DLQ is bypassed.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Matter/MatterSourceTask.cs` L106: PollAsync's 'catch (Exception) { // Log and continue }' has no logging, so a permanently unreachable controller looks identical to an idle one.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Matter/MatterSourceTask.cs` L77: The REST endpoints assumed here ('GET {controller}/api/nodes', 'POST {controller}/api/command') match no standard Matter controller (python-matter-server speaks WebSocket), so the connector only works against a custom, unspecified REST bridge.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Matter/MatterConnectorConfig.cs` L11: 'matter.commissioner.node.id' is defined as a config constant but is neither declared in any ConfigDef nor read by any task.

### Mattermost  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Mattermost/MattermostSourceTask.cs` L84: The per-channel cursor lives only in the in-memory _lastPostTimes dictionary and is never restored from offset storage; on restart 'since' resets to 'DateTimeOffset.UtcNow.AddMinutes(-5)', so any downtime longer than 5 minutes silently skips the posts in between (and shorter downtime re-emits duplicates) despite SourceOffset being populated per record.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Mattermost/MattermostSourceTask.cs` L75: The ConfigDef documents 'mattermost.channel.ids' as '(empty for all)' (MattermostSourceConnector.cs:26), but PollAsync returns immediately when the filter is empty ('if (_httpClient == null || _channelFilter == null || _channelFilter.Count == 0) return records;') — the advertised all-channels mode polls nothing.

### Mirror  (tests: behavioral)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Mirror/MirrorSourceTask.cs` L69: Start only seeds '_currentOffsets[(topic, 0)] = 0' with the comment 'For now, start from partition 0 offset 0 / A full implementation would discover all partitions' — partitions >= 1 of every topic are silently never replicated (data loss on any multi-partition topic).
- **bug** `src/Kuestenlogik.Surgewave.Connector.Mirror/MirrorSourceTask.cs` L69: Offsets always start at 0 and are never restored from offset storage (Context.OffsetStorageReader is unused), so every task restart re-replicates all topics from the beginning, flooding the target with duplicates.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Mirror/MirrorCheckpointTask.cs` L89: PollAsync only iterates '_checkpointStore.All', but the store is filled exclusively via UpdateCheckpoint (line 137) which no production code ever calls (grep hits only its definition), and the connected _sourceClient (lines 59-60) is never used to read consumer-group offsets — the checkpoint connector can never emit a single record.
- **stub** `src/Kuestenlogik.Surgewave.Connector.Mirror/Failover/FailoverManager.cs` L11: FailoverManager and CircuitBreaker are referenced only by their unit tests; no connector or task code path constructs them, so the shipped failover feature is dead code.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Mirror/MirrorSourceTask.cs` L119: 'Headers = null // Native protocol doesn't expose headers directly yet' — record headers are silently dropped during replication.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Mirror/MirrorSourceConnector.cs` L55: 'source.security.protocol' / SASL configs are declared and forwarded via TaskConfigs (lines 173-184), but MirrorSourceTask connects with plain 'new SurgewaveNativeClient(host, port)' and never reads them — security settings are silently ignored.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Mirror/MirrorHeartbeatTask.cs` L74: The configured 'heartbeats.topic' is stored in _heartbeatsTopic (line 28) but the emitted record uses '_policy.HeartbeatTopic(...)' instead; MirrorCheckpointTask has the same dead '_checkpointsTopic' (line 38 vs the policy call at line 120).

### Nanomsg  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Nanomsg/NanomsgSinkTask.cs` L107: PutAsync wraps `sendSocket.Send(record.Value)` in `catch (Exception) { // Log and continue }` with no actual logging or error raise, so a failed send silently drops the record while offsets advance — data loss.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Nanomsg/NanomsgSourceTask.cs` L106: PollAsync has a blanket `catch (Exception) { // Log and continue }` that logs nothing; a persistent socket failure degenerates into silent empty polls with no error ever surfaced.

### Nats  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Nats/NatsSourceTask.cs` L129: Stop() drains `_pendingAcks` and acks every message ('Ack any remaining pending messages') even though CommitAsync never ran for them; messages fetched from JetStream but not yet durably written are acked on shutdown and never redelivered — at-least-once violation / data-loss window.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Nats/NatsSourceConnector.cs` L50: `tls.enabled`, `reconnect.wait.ms` and `max.reconnects` are declared in both source (lines 50-57) and sink (NatsSinkConnector.cs:36-43) ConfigDefs but neither task ever reads them — setting tls.enabled=true silently has no effect on the NatsOpts.

### Nats.ObjectStore  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Nats.ObjectStore/NatsObjectStoreSinkTask.cs` L96: PutAsync waits at most ~5s for the fire-and-forget init and then `if (!_isInitialized) return;` — the whole batch is silently discarded while offsets advance, causing permanent data loss whenever connection/bucket setup failed or is slow (init failure itself is swallowed at line 80: 'Log and continue - will retry on put', but nothing retries).
- **bug** `src/Kuestenlogik.Surgewave.Connector.Nats.ObjectStore/NatsObjectStoreSinkTask.cs` L198: Each record's Put/Delete is wrapped in `catch (Exception) { // Log and continue }` with no logging and no error raise, so failed object writes are silently dropped — data loss.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Nats.ObjectStore/NatsObjectStoreSourceTask.cs` L66: Start fire-and-forgets `_ = InitializeAsync(...)` whose `catch (Exception) { // Log and continue }` (line 91) swallows all connect/GetObjectStore errors without logging or Context.RaiseError, so on failure the source silently polls empty forever; the watcher loop (lines 115/125) swallows exceptions the same way.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Nats.ObjectStore/NatsObjectStoreSourceConnector.cs` L33: `nats.credentials.file` is declared in the source ConfigDef but the task never reads it (only username/password/token), and the sink declares `nats.objectstore.chunk.size` (NatsObjectStoreSinkConnector.cs:41) that the sink task never reads.

### Neo4j  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Neo4j/Neo4jSourceTask.cs` L152: Incremental paging uses `WHERE n.{ts} > $lastTimestamp ... LIMIT {maxRows}` with `_lastTimestamp` taken from the last row of the batch; rows sharing that timestamp beyond the LIMIT boundary are permanently skipped (strictly-greater cursor bug — data loss).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Neo4j/Neo4jSourceTask.cs` L30: `_lastTimestamp` lives only in memory and is never restored from offset storage (no OffsetStorageReader usage) although SourceOffset timestamp/elementId values are dutifully emitted — every restart re-reads all matching nodes from the beginning.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Neo4j/Neo4jSourceTask.cs` L133: PollAsync catches all exceptions and only does `Console.Error.WriteLine($"Neo4j poll error: {ex.Message}")`, returning an empty batch — poll failures are invisible to the Connect framework.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Neo4j/Neo4jSinkTask.cs` L151: `labelExpression` derived from the `neo4j.node.label.field` config is computed but never used; both MERGE and CREATE hard-code the static `_label` (the CREATE branch even admits 'For simplicity, we'll use the static label'), so the declared dynamic-label config silently does nothing.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Neo4j/Neo4jSinkTask.cs` L214: ParseRecordValue's bare `catch { return null; }` makes PutAsync silently skip any record whose value is not valid JSON — silent drop with no error raised and no dead-letter path.

### Nsq  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Nsq/NsqSourceTask.cs` L96: HandleMessage claims 'This blocks if the channel is full, applying backpressure' but calls `_messageChannel.Writer.TryWrite(message)` and ignores the false result: when the bounded channel (1000) is full the message is dropped without Finish/Requeue, each redelivery burns an attempt, and after MaxAttempts NSQ hands it to the empty no-op `LogFailedMessage` (line 102) — permanent data loss under sustained backlog.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Nsq/NsqSourceConnector.cs` L34: `nsq.tls.enabled` and `nsq.tls.insecure.skip.verify` are declared in both source (lines 34-35) and sink (NsqSinkConnector.cs:29-30) ConfigDefs but neither task ever reads them — TLS settings silently do nothing.

### Ollama  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Ollama/OllamaSinkTask.cs` L276: Webhook delivery discards the `PostAsync` result with no status check (no EnsureSuccessStatusCode), so 4xx/5xx responses count as success and the computed embedding/completion is silently lost; only transport-level HttpRequestException reaches the `catch` that raises an error but still does not fail the batch.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Ollama/OllamaSinkTask.cs` L287: Without a webhook the only output path is `Console.WriteLine(...)` truncated to 200 chars ('Default: log to console for debugging') — in the default configuration all Ollama results are computed and then discarded; there is no path to write results back to a topic.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Ollama/OllamaSinkConnector.cs` L52: ConfigDef declares `batch.timeout.ms` (line 52) and `output.format` (line 62) but OllamaSinkTask never reads either — there is no batch timeout (records sit buffered until batch.size or FlushAsync) and the json/merge format choice has no effect.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Ollama/OllamaSinkTask.cs` L45: `float.Parse(GetConfig(..., TemperatureConfig, ...))` uses the current culture; on a comma-decimal locale (e.g. de-DE) a configured "0.7" parses as 7, silently maxing out the temperature.

### OpenAI  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.OpenAI/OpenAISinkTask.cs` L725: Webhook delivery is fire-and-forget: PostAsync's response status is never checked (no EnsureSuccessStatusCode) and HttpRequestException is converted to RaiseError while PutAsync still returns normally, so offsets commit for results that were never delivered.
- **stub** `src/Kuestenlogik.Surgewave.Connector.OpenAI/OpenAISinkTask.cs` L736: Without webhook.url the sink's entire output path is 'Console.WriteLine($"[OpenAI] ...")' — processed embeddings/completions are discarded (the ConfigDef even documents 'logs to console if not set').
- **bug** `src/Kuestenlogik.Surgewave.Connector.OpenAI/OpenAITransformTask.cs` L130: PutAsync catches all per-record exceptions ('catch (Exception ex) { Context?.RaiseError?.Invoke(ex); }') and continues, so a record whose chat call or produce to the output topic failed still gets its offset committed by the runner.
- **smell** `src/Kuestenlogik.Surgewave.Connector.OpenAI/OpenAISinkConnector.cs` L98: 'batch.timeout.ms' (BatchTimeoutMsConfig, 'Maximum time to wait for batch to fill') is declared in the ConfigDef but never read by OpenAISinkTask.
- **smell** `src/Kuestenlogik.Surgewave.Connector.OpenAI/OpenAITransformTask.cs` L123: Standalone fallback producer always sends to partition 0 ('_ownProducer.Messaging.SendAsync(_outputTopic, 0, ...)'), and Start blocks with 'ConnectAsync(CancellationToken.None).GetAwaiter().GetResult()'; Stop() nulls the chat client but never disposes _ownProducer (only Dispose does).

### Orleans  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Orleans/OrleansSinkTask.cs` L111: publish.timeout.ms is dead code: 'timeoutCts.CancelAfter(_publishTimeoutMs)' is created but the token is never passed to 'await _stream.OnNextAsync(record.Value)', so a stuck stream publish hangs PutAsync indefinitely.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Orleans/OrleansSinkConnector.cs` L39: 'serialization.type' is declared in both the sink and source ConfigDefs but never read by either task.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Orleans/OrleansSourceTask.cs` L92: OnErrorAsync is a swallow: the comment says 'Log and continue' but the method body only returns Task.CompletedTask — stream errors vanish without any log or RaiseError.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Orleans/OrleansSourceTask.cs` L32: Events are buffered in an unbounded ConcurrentQueue with no backpressure, and the stored sequence-token offset is never used to resume the subscription on restart, so undelivered buffered events are lost across restarts; both tasks also hardcode 'AddMemoryStreams' as the only supported provider.

### Parquet  (tests: behavioral)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Parquet/ParquetSinkTask.cs` L180: Rolling mode loses data: append is only true when '_outputMode == OutputModeAppend', so in rolling mode every flush runs 'File.Create(_currentFilePath)' on the same un-rotated file, truncating all rows from previous flushes — and the Connect runner flushes after every record.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Parquet/ParquetSourceTask.cs` L231: delete.after.read/move.after.read runs 'File.Delete(filePath)' in HandleEndOfFile as soon as the file is read into the poll batch — before the records are produced and offsets committed — so a crash or produce failure after deletion loses the data permanently.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Parquet/ParquetSinkTask.cs` L254: Append-mode merge swallows read failures with a bare 'catch { // If we can't read the existing file, start fresh }', silently discarding all previously written rows when the existing file is unreadable.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Parquet/ParquetSinkTask.cs` L152: Schema is inferred once from the first buffered record with every column as DataField<string> ('Always use string type for simplicity'); later records' extra fields are silently dropped and all typing is lost — the round-trip test itself asserts id comes back as the string "42".
- **smell** `src/Kuestenlogik.Surgewave.Connector.Parquet/ParquetSinkTask.cs` L185: Append mode re-reads the entire existing file into memory and rewrites it on every flush; combined with the runner's per-record FlushAsync this is O(n^2) in file size and unbounded in memory.

### SignalR  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.SignalR/SignalRSourceTask.cs` L104: Three On(...) handlers are registered for the same hub method with different signatures (On<string,string> line 104, On<string> line 121, On<object> line 138); the ASP.NET Core SignalR client binds a method's incoming arguments to a single handler signature, so this arity-fallback cannot work — messages either fail to bind and are dropped with a binder error, or fire multiple compatible handlers producing duplicate records.
- **bug** `src/Kuestenlogik.Surgewave.Connector.SignalR/SignalRSourceTask.cs` L117: _recordChannel.Writer.TryWrite(record) has its return value ignored on a bounded(10000) channel — BoundedChannelFullMode.Wait applies only to WriteAsync, so when the buffer is full TryWrite returns false and incoming hub messages are silently discarded.
- **smell** `src/Kuestenlogik.Surgewave.Connector.SignalR/SignalRSinkTask.cs` L147: FlushBufferAsync's bare 'catch' assumes any batch-invoke failure means 'batch method doesn't exist' and resends every buffered record individually, swallowing the real error and duplicating messages the server may already have processed.

### Surgewave.Bridge  (tests: none)
- **stub** `src/Kuestenlogik.Surgewave.Connector.Surgewave.Bridge/SurgewaveBridgeSourceTask.cs` L270: CommitRecord is an empty stub ('Offset tracking for failover could be implemented here') and InitializeAsync seeds _currentOffsets from 0 or GetLatestOffsetAsync without ever consulting Context.OffsetStorageReader, so every task restart re-replicates all topics from the beginning (or, with start.from.latest, silently skips everything produced while it was down).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Surgewave.Bridge/SurgewaveBridgeSourceConnector.cs` L47: 'offset.tracking.enabled' and 'offset.sync.interval.ms' are declared in the source ConfigDef (and 'producer.linger.ms' in the sink's, SurgewaveBridgeSinkConnector.cs line 40) but no task code ever reads any of them — offset tracking is advertised yet unimplemented.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Surgewave.Bridge/SurgewaveBridgeSourceTask.cs` L164: Per-partition receive errors are swallowed by 'catch (Exception) { // Log and continue }' without any actual logging, hiding persistent replication failures.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Surgewave.Bridge/SurgewaveBridgeSinkTask.cs` L54: '_preservePartitions ? record.Partition : 0' routes all traffic to partition 0 when partition preservation is disabled instead of distributing (no partitioner/round-robin).

### Teams  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Teams/TeamsSourceTask.cs` L77: PollAsync fetches only 'QueryParameters.Top = 50' newest messages with no NextLink paging, then advances _lastPollTime to the newest CreatedDateTime (lines 122-129); if more than 50 messages arrive in one interval, the older ones are permanently skipped.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Teams/TeamsSourceTask.cs` L47: The cursor is in-memory only: Start sets '_lastPollTime = DateTimeOffset.UtcNow' and never reads Context.OffsetStorageReader (which 28 sibling connectors use), so messages posted while the connector was stopped are never delivered even though SourceOffset is emitted per record.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Teams/TeamsSourceTask.cs` L115: Reply fetches are wrapped in 'catch { // Ignore reply fetch errors }'; a transient Graph failure silently drops those replies forever because _lastPollTime advances past them in the same poll.

### Telegram  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Telegram/TelegramSourceTask.cs` L76: Updates are only buffered in the in-memory '_pendingRecords.Enqueue(record)' queue while Telegram.Bot's StartReceiving auto-advances the getUpdates offset (acknowledging them to Telegram); a crash before PollAsync drains the queue permanently loses those messages (at-most-once), and no offset is ever restored.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Telegram/TelegramSourceTask.cs` L81: HandleErrorAsync's body is empty except the comment '// Log error and continue' — polling/auth failures (e.g. bad bot token) are swallowed with no log and no Context.RaiseError, so the source silently produces nothing.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Telegram/TelegramSourceConnector.cs` L35: ConfigDef declares 'telegram.polling.mode' (default 'long-polling', implying a webhook mode) but the key is never read anywhere and the WebhookUrl/WebhookPort constants are dead — only long polling exists.

### VectorStore  (tests: behavioral)
- **stub** `src/Kuestenlogik.Surgewave.Connector.VectorStore/VectorStoreSourceTask.cs` L50: PollAsync always returns [] after a 100ms delay ('In a real implementation, this would consume from the query topic'), and the only real search path, internal ProcessQuery (line 63), is invoked only by tests — the framework calls only PollAsync and TaskContext has no consumer, so the source connector can never emit search results at runtime.
- **smell** `src/Kuestenlogik.Surgewave.Connector.VectorStore/VectorStoreSourceConnector.cs` L310: Required config 'query.topic' is declared and validated but never consumed anywhere — the task stores it in _queryTopic (VectorStoreSourceTask.cs:29) and never uses it.
- **smell** `src/Kuestenlogik.Surgewave.Connector.VectorStore/VectorStoreSinkTask.cs` L110: FlushAsync writes full snapshots to 'persistence.topic' (advertised as 'persist vectors to a compacted topic for durability') but nothing ever reads them back on Start, so the in-memory store is empty after every restart — durability is write-only.

### Weather  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Weather/WeatherSourceTask.cs` L63: double.Parse(config[Latitude]) / double.TryParse (line 50) without InvariantCulture — on a de-DE-locale host '52.52' parses as 5252 (dot is the group separator), silently producing weather for wrong coordinates.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Weather/WeatherSourceTask.cs` L107: catch (Exception) with only the comment '// Log and continue with next location' — no logging exists, so a bad API key or geocode failure makes the connector silently produce nothing forever.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Weather/WeatherSourceConnector.cs` L66: ConfigDef documents locations as 'city names or lat,lon', but the task splits the list on ',' and coordinates on ';' (WeatherSourceTask.cs:46-49), so the documented '52.5,13.4' becomes two bogus city-name entries.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Weather/WeatherSourceConnector.cs` L62: Provider 'nws' and data type 'alerts' are advertised in ConfigDef but neither is implemented — the task only branches openweathermap-vs-else(open-meteo), so 'nws' silently uses Open-Meteo and 'alerts' fetches nothing.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Weather/WeatherSourceTask.cs` L85: City-name locations are re-geocoded on every poll (the lat==0 sentinel entry is never updated with the geocode result), and GeocodeLocationAsync throws for any provider except openweathermap-with-key (lines 118-131), so open-meteo + city name silently never produces records.

### Wikipedia  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Wikipedia/WikipediaSourceTask.cs` L149: Recent-changes cursor is inverted: 'url += $"&rcstart={_lastChangeTimestamp}"' with MediaWiki's default rcdir=older, while _lastChangeTimestamp is overwritten per item and ends on the batch's OLDEST timestamp (line 194), so every subsequent poll enumerates progressively older history instead of new edits — changes mode walks backwards in time and never delivers new changes.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Wikipedia/WikipediaSourceTask.cs` L87: 'catch (Exception) { // Log and continue }' around the whole poll swallows all API/parse errors with no logging (e.g. mode=search without a validated query throws inside and the task silently produces nothing).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Wikipedia/WikipediaSourceConnector.cs` L75: 'wikipedia.include.content' is parsed into _includeContent (task line 42) but never used, and 'wikipedia.include.links', 'wikipedia.include.images', and 'wikipedia.categories' are Defined in ConfigDef but never read by the task.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Wikipedia/WikipediaSourceTask.cs` L29: _processedRevisions HashSet only ever grows (added at line 193, never pruned), so changes mode leaks memory unboundedly over the connector lifetime.


## functional (26)

### Aws.Sns  (tests: config-only)
- no findings

### Azure.Queue  (tests: config-only)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Azure.Queue/QueueStorageSourceTask.cs` L124: Opt-in delete.after.read=true deletes the message inside PollAsync before the records are returned/committed, so a crash loses the message; the default (commit-based deletion in CommitAsync) is correct at-least-once.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Azure.Queue/QueueStorageSinkTask.cs` L169: GetMessageContent's catch branch ('If not valid UTF-8, base64 encode') is unreachable because Encoding.UTF8.GetString never throws — binary payloads get mangled with U+FFFD replacement characters instead of being base64-encoded.

### Csv  (tests: behavioral)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Csv/CsvSourceTask.cs` L110: When none of the configured files exist, OpenNextFile() exhausts the list and returns false without ever setting _endOfFile, so PollAsync returns an empty list immediately on every call — a tight busy loop with no delay until a file appears (which it never re-checks, since the index is exhausted).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Csv/CsvSourceTask.cs` L242: With delete.after.read, HandleEndOfFile() deletes the source file at EOF before the last polled batch has been committed/durably produced, so a crash in that window loses both the records and the source file (inherent risk of the opt-in feature, but deletion is not commit-gated).

### DeepL  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.DeepL/DeepLSinkTask.cs` L377: Webhook delivery result is never checked ('await _httpClient.PostAsync(new Uri(_webhookUrl), content)' with no EnsureSuccessStatusCode and no logging), so a 4xx/5xx from the webhook silently discards the translation result while the record is acked.
- **smell** `src/Kuestenlogik.Surgewave.Connector.DeepL/DeepLSinkConnector.cs` L64: 'batch.timeout.ms' is declared in ConfigDef ("Maximum time to wait for batch to fill") but DeepLSinkTask never reads BatchTimeoutMsConfig - batches are only size-based.

### Excel  (tests: behavioral)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Excel/ExcelSourceTask.cs` L285: With delete.after.read/move.after.read enabled, HandleEndOfFile runs 'File.Delete(filePath)' / 'File.Move(...)' inside PollAsync before the just-read records are even returned to the framework, so a crash or produce failure after the delete loses the file's data irrecoverably.
- **bug** `src/Kuestenlogik.Surgewave.Connector.Excel/ExcelSinkTask.cs` L224: Rolling files are named via '${timestamp}' formatted 'yyyyMMddHHmmss' (seconds resolution), so rolling more than once per second for the same topic produces the same path and 'SaveAs' silently overwrites the previous rolled file's rows.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Excel/ExcelSourceTask.cs` L252: OpenNextFile's bare 'catch { CloseCurrentFile(); return OpenNextFile(); }' silently skips corrupt/unreadable workbooks with no log, hiding that a configured file was never ingested.

### Flink  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Flink/FlinkSinkConnector.cs` L139: ParseCommand maps a null/empty record value and any JSON parse failure (catch at line 162) to 'new FlinkCommand { Action = _defaultAction }', so with a configured default JAR id every tombstone or malformed record triggers an actual job submission instead of being skipped.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Flink/FlinkSourceConnector.cs` L80: '_lastJobStates' grows without bound - entries for finished/cancelled jobs are never evicted, a slow leak on long-running clusters with many short jobs.

### Gcp.Storage  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Gcp.Storage/GcsSourceTask.cs` L154: With delete.after.read=true (default false) the object is DeleteObjectAsync'd inside PollAsync immediately after parsing, before the records are produced or committed to Surgewave — a crash between delete and produce permanently loses the file; deletion belongs in CommitAsync/CommitRecord.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Storage/GcsSourceConnector.cs` L19: 'gcs.project.id' is declared in both the source and sink ConfigDefs (GcsSinkConnector.cs:19) but never read by either task — StorageClient is created without it.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Gcp.Storage/GcsSourceTask.cs` L26: Dedup relies on the in-memory '_processedObjects' HashSet that grows unboundedly and is lost on restart, while every poll re-lists the entire bucket prefix via ListObjects — memory and API cost grow with bucket size for the lifetime of the task.

### Generator  (tests: behavioral)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Generator/GeneratorSourceTask.cs` L56: generator.message.format is declared and validated in the connector ('json'/'string'/'bytes') and read into _messageFormat, but the field is never used afterwards - output is always the template string as UTF-8 regardless of format.

### Kafka.Bridge  (tests: none)
- **stub** `src/Kuestenlogik.Surgewave.Connector.Kafka.Bridge/KafkaBridgeSourceTask.cs` L90: 'topics.pattern' is declared in the ConfigDef and accepted as sufficient by connector validation (KafkaBridgeSourceConnector.cs:77 'hasTopics || hasPattern'), but the task throws NotSupportedException("Topic pattern subscription requires configuration at consumer config level") — a pattern-only config passes validation and then always fails at task start.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Kafka.Bridge/KafkaBridgeSourceTask.cs` L129: catch (ConsumeException) with only the comment '// Log and continue' — no logging exists, so consume errors (auth failures, unknown topics) are silently swallowed and the task spins returning empty batches.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Kafka.Bridge/KafkaBridgeSourceTask.cs` L100: PollAsync calls _consumer.Consume(...) once and returns at most one record per poll; the declared 'max.poll.records' config (KafkaBridgeSourceConnector.cs:43) is never read — dead config and a hard throughput ceiling.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Kafka.Bridge/KafkaBridgeSinkTask.cs` L109: 'await _producer!.ProduceAsync(...)' inside the per-record loop waits for each broker ack before sending the next message, defeating the configured linger.ms/batch.size batching (delivery is still correct, just serialized).

### MongoDB  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.MongoDB/MongoDbSourceTask.cs` L100: Poll-mode offset restore does '_lastPolledValue = BsonValue.Create(valueString)' — always a BSON string — although the stored value came from ToJson() of the original type; a Gt filter comparing that string against a numeric/ObjectId/date poll.field (lines 264-266) matches nothing under BSON type ordering, so after a restart the poll source silently stops emitting for any non-string poll field.
- **smell** `src/Kuestenlogik.Surgewave.Connector.MongoDB/MongoDbSinkTask.cs` L128: 'catch (BsonException) { // Skip invalid BSON documents }' drops unparseable records silently with no log entry and no DLQ, so malformed input vanishes without trace.
- **smell** `src/Kuestenlogik.Surgewave.Connector.MongoDB/MongoDbSourceTask.cs` L279: In PollCollectionAsync, CreatePollRecord(doc) reads _lastPolledValue before it is updated for the current doc (line 280), so each record's SourceOffset carries the previous record's poll value — one guaranteed duplicate after every resume.
- **smell** `src/Kuestenlogik.Surgewave.Connector.MongoDB/MongoDbSourceTask.cs` L224: 'fullDocument', 'documentKey' and 'updatedFields' are embedded as ToJson() strings inside the JSON payload, producing double-encoded JSON instead of nested objects for downstream consumers.

### Mqtt  (tests: config-only)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Mqtt/MqttSourceTask.cs` L127: OnMessageReceived enqueues the message into an in-memory Queue and returns Task.CompletedTask, at which point MQTTnet auto-acknowledges (PUBACK) to the broker — QoS-1 messages sitting in the buffer are acknowledged before any durable write to Surgewave and are lost if the process crashes before PollAsync drains them.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Mqtt/MqttSourceTask.cs` L96: No DisconnectedAsync/reconnect handling exists: after a broker disconnect the source silently receives nothing while PollAsync keeps returning empty batches; the sink likewise never reconnects and throws 'MQTT client is not connected' (MqttSinkTask.cs:125) until its records exhaust retries into the DLQ.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Mqtt/MqttSourceConnector.cs` L46: 'mqtt.tls.client.cert.path' and 'mqtt.tls.client.cert.password' are declared in both ConfigDefs (also MqttSinkConnector.cs:48-50) and asserted present by the tests, but neither task ever reads them — client-certificate TLS is silently ignored.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Mqtt/MqttSourceTask.cs` L24: The message buffer Queue is unbounded — the constant DefaultMaxPendingMessages = 10000 (MqttConnectorConfig.cs:53) exists but is never applied — so a publisher outpacing the 100 ms poll cycle grows memory without limit.

### Nats.Kv  (tests: config-only)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Nats.Kv/NatsKvSourceTask.cs` L213: The `entryData` anonymous object under the comment 'Serialize entry metadata to JSON for the value' is built but never used — dead code; the record actually carries the raw `entry.Value`.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Nats.Kv/NatsKvSourceTask.cs` L232: SourceOffset (local counter + KV revision) is emitted but never restored (no OffsetStorageReader usage anywhere), so after a restart the watcher starts fresh and previously delivered entries can be re-emitted.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Nats.Kv/NatsKvSourceTask.cs` L56: The task reads `kv.poll.interval.ms` (NatsKvConnectorConfig.PollIntervalMs) but the key is not declared in NatsKvSourceConnector's ConfigDef, so it is invisible/unvalidated in the UI.

### Qdrant  (tests: config-only)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Qdrant/QdrantSinkTask.cs` L194: Records without the configured vector field (or with an unparsable vector) return null from ParseRecordToPoint with no RaiseError or log, so they are silently dropped while their offsets commit.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Qdrant/QdrantSinkTask.cs` L93: FlushBufferAsync clears the buffer ('_buffer.Clear()') before the upsert succeeds; under a batching runner an upsert failure on the final retry attempt throws with the batch already discarded, so only the runner's current record is re-delivered.

### RabbitMQ  (tests: config-only)
- **smell** `src/Kuestenlogik.Surgewave.Connector.RabbitMQ/RabbitMQSinkTask.cs` L203: FlushAsync is a no-op without publisher confirms — the code itself admits it ('Could enable publisher confirms for guaranteed delivery'), so offsets can be committed while the last publishes are still unconfirmed in-flight; publish errors do propagate though, so the loss window is only a broker/connection crash.
- **smell** `src/Kuestenlogik.Surgewave.Connector.RabbitMQ/RabbitMQSourceTask.cs` L66: Tasks read config keys that no ConfigDef declares — 'rabbitmq.poll.timeout.ms' (source line 66), 'rabbitmq.tls.cert.file', 'rabbitmq.connection.name', 'rabbitmq.dead.letter.routing.key' — while TlsKeyFile/TlsCaFile constants (RabbitMQConnectorConfig.cs:19-20) are never read anywhere.
- **smell** `tests/Kuestenlogik.Surgewave.Connector.RabbitMQ.Tests/RabbitMQSinkTaskTests.cs` L26: Task tests attempt a live Start() against a non-running broker and swallow any exception whose message contains 'connect'/'refused' inside Record.Exception, making most asserts near-tautological (only config parsing is really exercised).

### Redis.Scan  (tests: none)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Redis.Scan/RedisScanSourceTask.cs` L43: `_includeValue = config.TryGetValue(...) && iv == "true"` makes the effective default false and the comparison case-sensitive, while the ConfigDef (RedisScanSourceConnector.cs:27) declares DefaultIncludeValue = true.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Redis.Scan/RedisScanSourceTask.cs` L66: Progress is only the in-memory `_seenKeys` set (cleared wholesale at >100k, line 97-100) and every poll re-enumerates the keyspace via `_server.Keys` from scratch — restarts re-emit all keys and each poll is O(keyspace); acceptable for a snapshot-style scan source but worth knowing.

### Script  (tests: behavioral)
- **stub** `src/Kuestenlogik.Surgewave.Connector.Script/ScriptTransformConnector.cs` L43: ConfigDef declares batch.size and references (lines 43-54) but no task ever reads them — the assembly-references setting has no effect on script compilation (WithReferences uses a fixed list).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Script/ScriptTransformTask.cs` L46: process.mode is parsed into _processMode but never consulted; PutAsync always iterates per record even when "batch" is configured.

### Sequence  (tests: behavioral)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sequence/SequenceSourceConnector.cs` L34: The completion.behavior dropdown offers options ["stop", "restart", "idle"] but Start validates against only stop/restart and throws ArgumentException for the offered "idle" value.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sequence/SequenceSourceTask.cs` L263: CreateSourceTask returns null on any failure (unresolvable task.class, ctor exception) and StartCurrentSource silently accepts it, so a misconfigured child leaves PollAsync returning [] forever with no error raised (Context.RaiseError unused).
- **smell** `src/Kuestenlogik.Surgewave.Connector.Sequence/SequenceSourceTask.cs` L219: Child tasks are initialized with a fresh empty 'new TaskContext()', so wrapped sources lose the runtime's OffsetStorageReader/Producer/RaiseError (e.g. a child Sftp source can never restore its offsets).

### Slack  (tests: config-only)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Slack/SlackSourceTask.cs` L40: The event buffer is CreateBounded(1000) with FullMode = BoundedChannelFullMode.DropOldest, so under backpressure the oldest unpolled Slack events are silently discarded with no metric or log.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Slack/SlackSinkConnector.cs` L55: ConfigDef declares slack.markdown (line 55) and slack.batch.size (line 71) but SlackSinkTask never reads either — messages are always sent one at a time and the Markdown flag is never applied.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Slack/SlackSourceConnector.cs` L27: slack.signing.secret is declared but read nowhere, and slack.socket.mode is only used for connector-side validation — SlackSourceTask.EnsureConnectedAsync always connects via Socket Mode and unconditionally requires an app token, so socket.mode=false is not honored.

### Smtp  (tests: config-only)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Smtp/SmtpSinkTask.cs` L375: AddCustomHeaders hardcodes root.TryGetProperty("headers", ...) — the declared and parsed smtp.headers.field (_headersField, line 81) is never used, so a custom headers field name is silently ignored.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Smtp/SmtpSinkTask.cs` L129: Records with invalid JSON are dropped by an empty 'catch (JsonException)' and records without resolvable recipients return null from BuildMessage (lines 154-155) and are skipped — both silently, with no log or dead-letter path.

### SocketServer  (tests: behavioral)
- **bug** `src/Kuestenlogik.Surgewave.Connector.SocketServer/SocketServerSourceConnector.cs` L46: Framing Select options are declared as ["newline", "length-prefixed", "raw"] but Start() only accepts 'line'/'delimiter'/'length-prefix'/'raw' (lines 123-128), so two of the three dropdown choices throw 'Invalid framing mode' and 'delimiter' mode is unreachable from the UI.
- **smell** `src/Kuestenlogik.Surgewave.Connector.SocketServer/SocketServerSourceTask.cs` L256: 'stream.ReadTimeout = _idleTimeoutMs' is a no-op because the handler only uses ReadAsync (NetworkStream/SslStream ReadTimeout applies to synchronous reads), so the declared idle.timeout.ms never disconnects idle clients.
- **smell** `src/Kuestenlogik.Surgewave.Connector.SocketServer/SocketServerSourceConnector.cs` L39: 'socket.server.connection.timeout.ms' (line 39) and 'socket.server.send.buffer.size' (line 59) are declared in ConfigDef but the task's ParseConfig never reads either key.
- **smell** `src/Kuestenlogik.Surgewave.Connector.SocketServer/SocketServerSourceTask.cs` L49: _pendingRecords is an unbounded ConcurrentQueue fed by the receive loops with no cap or backpressure, so memory grows without limit if clients send faster than the framework polls.

### SocketStream  (tests: behavioral)
- **bug** `src/Kuestenlogik.Surgewave.Connector.SocketStream/SocketStreamSourceConnector.cs` L53: Same framing mismatch as SocketServer: dropdown options ["newline", "length-prefixed", "raw"] versus validated values 'line'/'delimiter'/'length-prefix'/'raw', so UI-selected 'newline' or 'length-prefixed' fails Start() with 'Invalid framing mode'.
- **bug** `src/Kuestenlogik.Surgewave.Connector.SocketStream/SocketStreamSourceTask.cs` L344: The field 'private int _pendingLength = -1;' is never reset when the read loop reconnects (ReadLoopAsync catch path only calls CloseConnection), so after a mid-frame disconnect in length-prefix mode the first bytes of the new connection are consumed as the remainder of the old frame, corrupting message framing.
- **smell** `src/Kuestenlogik.Surgewave.Connector.SocketStream/SocketStreamSourceTask.cs` L229: '_stream.ReadTimeout = _readTimeoutMs' has no effect because ReadMessagesAsync only calls ReadAsync, making the declared read.timeout.ms config a no-op.

### Spark  (tests: none)
- **bug** `src/Kuestenlogik.Surgewave.Connector.Spark/SparkSinkConnector.cs` L146: SparkSinkTask.PutAsync wraps each command in 'catch (Exception ex) { Console.Error.WriteLine(...) }' and continues, so on a transient Livy/Spark outage every job-submission command in the batch is dropped and its offset committed — no retry, rethrow, or dead-letter.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Spark/SparkSinkConnector.cs` L251: The responses of SubmitBatchAsync/SubmitApplicationAsync/CreateSessionAsync (containing batch/submission/session IDs) are awaited but discarded, so callers have no way to learn the ID needed for the connector's own kill/statement/delete actions.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Spark`: No test project exists for the Spark connector (no tests/Kuestenlogik.Surgewave.Connector.Spark.Tests), unlike most sibling connectors.

### Stdio  (tests: behavioral)
- no findings

### Tcp  (tests: behavioral)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Tcp/TcpSinkTask.cs` L110: Records larger than max.message.size are silently skipped ('continue; // Skip oversized messages') and thereby acknowledged without being sent and without any error signal or log.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Tcp/TcpSourceTask.cs` L36: _pendingRecords is an unbounded ConcurrentQueue fed by the accept/read loops; if the broker polls slower than TCP data arrives, memory grows without bound (the sibling Udp source uses a bounded channel with DropOldest instead).

### TextChunking  (tests: behavioral)
- **smell** `src/Kuestenlogik.Surgewave.Connector.TextChunking/TextChunkingConnector.cs` L236: TaskConfigs computes 'var numTasks = Math.Min(maxTasks, 1)' — always exactly one task — directly under the comment 'Text chunking can run multiple tasks for parallelism'.

### Udp  (tests: behavioral)
- **smell** `src/Kuestenlogik.Surgewave.Connector.Udp/UdpSourceTask.cs` L37: 'max.message.size' is declared in the source ConfigDef (UdpSourceConnector.cs:30) and parsed into _maxMessageSize, but the field is never referenced again — received datagrams are never checked against it.
- **smell** `src/Kuestenlogik.Surgewave.Connector.Udp/UdpSinkConnector.cs` L34: The sink ConfigDef declares BatchEnabled and BatchDelayMs (lines 34-38) but UdpSinkTask never reads either key and implements no batching — the settings are inert.

